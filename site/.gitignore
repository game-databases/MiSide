# deps / build
node_modules/
.next/
out/

# emitted build artifacts (regenerated every build by scripts/emit-artifacts.mjs)
public/api/
public/search/
public/llms.txt
public/feeds/

# permitted art classes copied from extracted/art at build time — never committed
public/cdn/

# next env — never commit a write key or a real .env
.env
.env*.local

# misc
*.tsbuildinfo
next-env.d.ts

# local logs
build-out.log
server.log
MiSide/site/*.log
