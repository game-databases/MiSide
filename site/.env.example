# PostHog product analytics
# Collector: Game Databases project 536998 (numeric id).
# Do not send events to New World Guide project 557596.
# Set the write key at deploy/runtime. Leave this placeholder empty in git —
# never commit a phc_ token or a real .env.
NEXT_PUBLIC_POSTHOG_KEY=
