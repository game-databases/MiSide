# MiSide DB — master TODO

Orchestrator-maintained. One line per unit; state in the tag:
`[ ]` todo · `[~]` in progress · `[x]` done · `[Q]` owner-gated · `[-]` dropped/deferred.
Target size: ≤80 points total, ~40 open as the standing goal. Evidence lives in
`docs/logs/`, verdicts in `docs/research/`, commits carry the details — this
board stays scannable, not narrative.

**Execution protocol** (promptForDB.md): every open line is a PIECE and runs the
full loop — PrepareTask (Scout→Documentator spec→Reviewer→Arbiter) → parallel
CodeWriter+TestWriter → parallel fresh Code/Test Reviewers → Arbiter (fresh
agents every round) → harsh side-by-side visual critic vs
new-world.guide/wikily/enka for user-facing surfaces. Agents launch as HARNESS
SUBAGENTS (Agent tool; owner directive 2026-08-26 — nested CLI retired);
every initial output gets ≥2 independent verifiers before acceptance.
Pack repo: `github.com/game-databases/MiSide` (child `49ab4e9`, superproject
gitlink `e64f2d00`) — commit in the CHILD from inside `MiSide/`; super-repo
only bumps the pin.

## Parallel lanes — what launches together

This section IS the wave plan: each tick, pick the next unit from one lane,
plus verifiers for whatever just landed. Concurrency caps learned from the
429 storms: steady-state ≤4 concurrent subagents (probe with a pair first
after any cap/storm window); storm-time ≤2 with the critical path serialized.
Never two writers on the same tree — site/ waves serialize against each
other; data-layer work runs alongside site/ freely.

- **LANE-A · contracts chain (critical path)**: F-DC1 fix → verifier pair →
  arbiter → contracts builder → derived re-pin. ⛓ Gates: tools trio,
  trackers, changelog viewer, UGC, relation cards.
- **LANE-B · scene maps**: viewer scout → spec loop → viewer build +
  interactions → entity-page map modules. Independent of LANE-A.
- **LANE-C · content**: guides section + news section + localized content
  layer — one content pipeline; scaffold together, translate together.
- **LANE-D · site hardening batch**: SEO/AEO completion, OG images, media
  perf, a11y, PWA — independent fixes; batch as ONE fixer wave + ONE
  reviewer pair, not five separate loops.
- **LANE-E · data tails**: logic-layer extraction, icons/UI export — zero
  overlap with site trees; unblock ⛓ tools later.

Wave pattern: one lane leader + one other lane's next unit in flight;
a verifier pair rides along per landed leader. UGC waits for LANE-C
(needs guides/news surfaces to comment on); LANE-D batch waits until no
other lane is mid-edit in `site/`.

## Phase 0 — bootstrap & research (closed)

- [x] Canon read in full: AGENTS, FRAMEWORK §1–§10, doctrines, promptForDB
- [x] Store facts pinned: appid 2527500 (+demo 2527520, DLC 3404450), 31 languages, released 2024-12-10
- [-] Demo client acquisition — dropped: moot while full client installed
- [x] Full client installed `A:\SteamLibrary\...\MiSide\`, buildId 19029065
- [x] T1 toolchain scout → `toolchain.md` + `data-acquisition.md` (Unity 2021.3.35f1, IL2CPP v29)
- [x] S1 game+competitor research + verifiers + fixer F-S1 (store=31 languages headline)
- [x] E1 hands-on toolchain probe — all 7 steps run for real (loc join key proven, census 2.277 GB)
- [x] T2 UI style scout double-PASS (dual-soul palette, motifs, credits 112 rows) → token source
- [x] Pipeline spec chain D1→R-P1→F-P1→A-P1→F-P2/F-P3 → FROZEN `836bef8`
- [x] `spec.md` freeze pack → A-SD1 SPEC_FROZEN (D3 gate preserved)

## Phase 1 — extraction (data layer closed)

- [x] Engine flavor detect + buildId pin (2021.3.35f1 · IL2CPP · v29 · 19029065)
- [x] Build wave: driver 11 modules green through 4 review rounds; T-W1 70-test suite accepted
- [x] Pipeline X-1→X-3 ALL STAGES GREEN: census 2,277,315,948 B · 140,380 objects · 163/163 assemblies · 34 locales / 180,148 records
- [x] Raw harvest principle-zero: art-export 11.4 GB / 72,909 files on `D:\unpacked_game_data\MiSide\` (C:-emergency MOVED-TO stubs)
- [x] Script decompile 163/163 assemblies + `_structure/` graphs
- [x] All-locale string tables 2,210 JSONLs / 180,148 records, recover-or-mark per A-S6
- [x] DS-1 characters dataset ACCEPTED (B-1, v-b1a+b1b PASS)
- [x] DS-2 achievements + endings trees ACCEPTED (B-2, v-b2a+b2b PASS)
- [x] DS-3 dialogue graphs ACCEPTED (B-3: 2,839 nodes / 3,776 edges; v-b3 caught D7 defect, r2 PASS)
- [x] DS-4/5 cartridges+minigames & documents+books ACCEPTED (B-4, B-5, dual PASS each)
- [x] DS-6 scenes/map-layer ACCEPTED (B-6: 24 scenes · 57 links · 986 POIs · markers no-orphan)
- [x] CLOSURE-1: relinks 25 files/1,159 edges + locale_availability 2,686 cells + PROOF §5 reconcile → PHASE-1 DATA LAYER CLOSED
- [x] Logic layer SPEC — ACCEPTED: D-LL1 → R-LL1(9) → A-LL1(two-tier sweep, join key K) → F-LL1 `76b96c2` → **V-LL1a+V-LL1b both APPROVE** (426 ln, zero numeric divergences; residuals: cloth-site source omission LOW-MED, 3 LOWs recorded in verdicts)
- [~] Logic-layer builder B-LL2 LANDED `299bba1` — V-LL2a APPROVE (no majors; law audit clean; dual-space no-silent-misjoin) → V-LL2b in flight → arbiter glance at latent suffix-first hazard (~15% collision propensity, blast radius limited) → then Ending Condition Explorer chain [LANE-E]
- [ ] Icons/UI 2D export completion + MEDIA-CATALOGUE audio/video rows finish
- [ ] Demo→full diff pass (deferred until needed)

## Phase 2 — data contracts

- [x] Entity schemas spec `docs/specs/data-contracts.mdx` — ACCEPTED: D-C1 → R-D-C1(6) → F-DC1 → V-DC1a/b + A-DC1 → F-DC2 → **V-DC2a+V-DC2b both APPROVE** (`0e9468c`, 545 ln; every census digit-reproduced by two independent verifiers). Residual: §6.1 stamp-prose true only for 3 families/16 rows (fix text in dc1-final-vB, fold at next doc pass)
- [x] Contracts artifacts — **ACCEPTED + RIDERS LANDED `185e904`** (V-CT2a/b APPROVE; fingerprints 84 incl. logic relinks; KEEP-PINNED §4.4; verify 22 gates green byte-stable) [⛓ OPEN for tools/tracker/changelog/UGC]
- [~] Derived artifacts re-pinned on contracts — map markers DONE (v2); **B-RP1 re-pinning search index + relation cards** onto accepted registries in flight
- [x] `missingdata.md` ledger — 83 entries classified
- [x] Pack infra: standalone repo `game-databases/MiSide` created + pushed; superproject gitlink `e64f2d00`

## Phase 3 — site build

- [x] Tools plan T-P1: 7 ideas scored /25, readiness-ordered (`tools-plan.md`)
- [x] Design tokens `design/tokens.css` (3-tier over T2 palette; hex-lint token-only; per-Mita accent spine)
- [x] App scaffold SB-1: Next.js + shadcn/ui re-tuned to game soul; R-SB2 APPROVE (SEO blockers fixed; parity ×34)
- [x] Chrome visual-critic loop rounds 1–4: CHROME_PASSES — trajectory 2–6 → **Home 8 · Entity 8 · Search 9 · RU 9 · AR 8 · Motion 9**; leads proven live vs enka/NWG/wikily (in-place search, corruption-as-state, locale URLs)
- [x] Home page 8/10 (transcript rail, arted grids)
- [x] Category list pages (5 section indexes + dialogue; banners + card art)
- [x] Global search results-in-place 9/10 (54 rows/8 kinds; Escape-restores) [DR-2026-08-22]
- [x] Site chrome i18n ×34 locales incl. RTL ar [DR-2026-08-20]
- [x] Character/Mita soul pages (registry palettes, module tabs, CorruptionHover h1s)
- [~] Entity-page density wave: NWG-class modules (calculator/3D/comments → tools/UGC phases), 13 artless cartridge portraits
- [ ] Chrome-loop debt: 2/15 art.ts provenance comments; portrait join extension; placeholder-tile taste ruling
- [~] Interactive scene maps per chapter/location — VC-MAP **FIX_ROUND_REQUIRED** avg 5.7 (bar 8): root = 0/70 markers plot (position-less rows dropped instead of rendering as LockedCells) + filters/counts/popstate/i18n defects → **F-MV4 fix round in flight** w/ own evidence shots → re-critic [LANE-B]
- [ ] Map module on location-bearing entity pages (two-way links)
- [x] Content-pipeline spec — **FROZEN** `ef022cc` (660 ln): scout→D-CT1→R-CT1(11)→A-CT1(12-item order)→F-CT1→dual verify→2 micro-fix rounds (C14 hardened ×3, base_sha16+C15). Erratum queued: missingdata.md CAR-5 `name_loc` drift. Builder next (machinery needs no OQ-1 ruling; mass authoring does)
- [~] Guides section build — **B-CT3 building M1–M5 machinery + EN/RU seeds** in flight (C14 enforcement + C15 staleness live). **OWNER GATE PENDING (OQ-1): translation authority — gates mass authoring volume/locales only** [LANE-C]
- [ ] News section (game + database news, localized)
- [ ] Tool: Ending Condition Explorer (top-scored, choice-tree calculator)
- [ ] Tool: Dev-Console Reference
- [ ] Tool: Owned Scene Map (readiness-ordered trio from T-P1)
- [ ] Tool: collectible & achievement trackers (checklist persistence)
- [ ] Changelog / patch-diff viewer (CORE differentiator)
- [ ] UGC: accounts, comments, corrections, screenshot uploads [DR-2026-08-19]
- [ ] SEO completion: JSON-LD entity markup, OG cards per template, IndexNow (hreflang matrix already green)
- [ ] AEO completion: llms.txt enrich + unauthenticated static JSON API
- [ ] Localized content layer: blog posts/articles translated to all 34 corpus locales [promptForDB Note#7]
- [ ] Search UX enrichment: typo tolerance + RU/EN synonyms in header search
- [ ] Media performance pass: next/image audit, responsive sizes, modern formats
- [ ] Accessibility pass: keyboard nav, focus management, aria on interactive chrome
- [ ] OG/share images per page template
- [ ] PWA/mobile polish

## Phase 4 — quality loop

- [ ] Visual critic rounds per new surface type (maps/tools/guides as they land)
- [ ] Fresh end-to-end smoother agent between major waves
- [ ] CWV smoke budgets: LCP<2.5s · interaction<200ms · CLS<0.1 per template × canonical locale
- [ ] curl-grep cross-link + quotable-block verification wired into CI
- [ ] Negative third-party-map grep in CI
- [ ] Banned-token copy sweep (anti-slop register rulings)

## Phase 5 — ship & after

- [ ] **OPS GATE — C: disk pressure**: was 370 MB at 07:30 (ENOSPC hit F-MV1 + scout mid-write), rebounded to 25 GB by ~08:15 after sibling churn. STANDING RULE: check `df /c` and require ≥10 GB before ANY site/ build; doc/data lanes always safe
- [Q] Domain/placement decision (owner gate Q3/D3; hub path until ruled)
- [ ] Deploy origin; llms.txt + JSON API verified non-empty in production
- [ ] Search Console + Bing WMT; answer-engine referral tracking
- [ ] Privacy-friendly analytics + error monitoring decision
- [ ] Post-launch bundle: embeds/Discord bot/MCP per D5 appetite
