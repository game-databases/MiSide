# Brief v-ds1b — Verifier: F-DS1B's five fixes to site-scaffold.mdx

Fresh verifier subagent of the MiSide orchestrator. CANNOT spawn agents;
`git diff` allowed; never write commands. NARROW scope.

## Check

Claims: `docs/logs/F-DS1B.log`. Review: `d-s1-review.mdx`.

1. **Gate map (M1):** source×space disposition table present; AC-S7 tests
   the conjunction (`inline AND world-assumed`); all five non-projecting
   classes have stated dispositions.
2. **Routing totality (M2):** nine kinds each have a route-with-source OR
   an explicit no-page ruling; none silent.
3. **S14 + §10:** sitemap partition == routes × ledger, zero-non-200
   invariant, JSON-LD ownership table assigned per piece.
4. **Advisories:** i18n rationale sentence present; 14-row label fixed.
5. Diff confined to this spec file.

## Deliverable

`docs/research/verifications/d-s1-vA.mdx`; final line exactly
`VERDICT: PASS` or `VERDICT: FAIL — <one line>`. ≤8 lines.
