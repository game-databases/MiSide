# Brief A-DS456 — Arbiter: build-readiness of the three wave-2 dataset specs (MiSide)

You are a FRESH Arbiter subagent of the MiSide orchestrator. CANNOT spawn
agents; `git log/diff` allowed; never write commands. You rule; you edit
nothing. Corpus: `C:\_reps\game-databases\MiSide\extracted\`.

## Case file

SPECS: `MiSide/docs/specs/dataset-cartridges.mdx`,
`dataset-documents.mdx`, `dataset-scenes.mdx`.
REVIEWS: `docs/research/verifications/ds4-review.mdx` (N-R 3),
`ds5-review-a.mdx` (evidence PASS), `ds5-review-b.mdx` (FAIL scoped to
AC-1). FIXES TO VERIFY YOURSELF (trust neither fixer's log nor prior
verifiers):
1. F-DS4 (`docs/logs/F-DS4.log`): hash-partition AC-7 (19/16/12/1),
   17 rows, DS-4 = single placement authority.
2. F-DS5 (`docs/logs/F-DS5.log`): AC-1 → 14 rows (#0–13), 21-equality
   delegated to DS-4 AC-2 by reference, row 0 = `mta`/level17/FlashTaker
   per measured `level17/FlashTaker.txt:10`.
SCENES: `dataset-scenes.mdx` unreviewed yet — YOU are its first reviewer:
sample ≥4 measurements (levelN↔Location(N−2) via World.nameLocation,
chapter lattice counts, POI position-truth labels, S9 stage proposal).

## Agenda items to resolve

- DS-4 §1 cross-ref overstatement of DS-5 placement column (11-row
  overlap scope) — require pre-emit edit or accept as emit-time check?
- Lens difference: ds5-review-a treats row-0 empty-key as already-
  ledgered; ds5-review-b demanded the mta fix (now applied). Confirm
  post-fix state is coherent with BOTH lenses.

## Mission

Per spec rule build-readiness (curatable without guessing; deferrals
fenced; ACs executable). Watch for NEW defects anywhere you read.

## Deliverable

`MiSide/docs/research/verifications/ds456-arbiter.mdx`; per-spec rulings;
final line exactly:
`RULING: ALL_APPROVED — launch builders B-4/B-5/B-6` or
`RULING: PARTIAL — <which blocked, numbered fixes>`. ≤14 lines.
