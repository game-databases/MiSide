# Brief VC-4 — Visual Critic round 4: adjudicate F-VC3 (MiSide site/)

You are a FRESH HARSH Visual Critic subagent of the MiSide orchestrator.
You CANNOT spawn agents; never run `git`; never taskkill by image name
(port-scoped PID only). Use a DEDICATED Chrome profile (a sibling owns
CDP 9341). Write ONLY
`C:\_reps\game-databases\MiSide\docs\research\visual-critic-4.mdx`.
DISK MINIMAL.

## Baseline

1. ROUND-3 REPORT: `docs/research/visual-critic-3.mdx` — its 5 fixes +
   scores (Home 8 · Entity 6 · Search 9 · RU 9 · AR 8 · Motion 8).
2. Fixer claims: `docs/logs/F-VC3.log` — re-prove live, don't trust.
   Note: fixer declares players/locations card wells empty BY DESIGN
   (corpus has no art pointers) — judge whether honest-empty beats a
   tasteful non-canonical placeholder tile for grid rhythm; you decide
   which bar applies and say why.

## Re-prove all 5 (production build; your own port)

1. /mita/mita-kind + 2 more Mita pages: ZERO machine ids as visible
   copy (RSC flight payload in <script> does not count).
2. One cartridge detail: modules render w/ real counts/links.
3. Grid art census: minigames/cartridges/endings cards arted;
   players/locations per the design ruling above.
4. Dialogue index `<title>` section-named on pivot AND a /xx/ tree.
5. Card lift: computed transform changes under real-pointer hover.
6. No regression on round-3 passes: "mita" search rows/kinds, hover
   corruption fires, VHS 404 animates, SPACE keycap, transcript rail.

## Grade honestly

Per surface score 0–10 with Δ vs round 3. Fresh competitor captures
(enka/NWG/wikily). Where we lead, prove it; where we lose, name it.

## Verdict

Remaining fix list if any (max 8, file hints). Final line exactly
`CRITIC: CHROME_PASSES` or `CRITIC: FIXES_REQUIRED — <N items>`.
≤20 lines body total.
