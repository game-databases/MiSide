# Brief v-ds123 — Verifier: all three dataset-spec fix rounds vs their reviews

Fresh verifier subagent of the MiSide orchestrator. CANNOT spawn agents;
`git diff` allowed, never write. You verify; you edit nothing.

## Check

Reviews: `docs/research/verifications/ds{1,2,3}-review.mdx`. Fixer claims:
`docs/logs/F-DS1.log`, `F-DS2.log`, `F-DS3.log`. Specs under
`MiSide/docs/specs/dataset-*.mdx`. Corpus: `C:\_reps\game-databases\MiSide\extracted\`.

Per spec, verify each claimed fix landed in text AND matches the review's
prescription (no over/under-fix):
1. **chars:** transform-T join replaces false equality; AC-7 asserts T;
   preview gloss marked unverified; locale_availability annotated planned.
2. **ach-end:** 353-all-wired census + sibling exclusion rows; `#line_index=`
   grammar everywhere (grep for any remaining bare `path:N` loc cites);
   45 collisions; 9-call chain named.
3. **dialogue:** union-of-carriers rule with failures listed + span levels;
   `line_index = indexString − 1` contract at all join definitions; blanks
   1,101 sums; cc@58 = 6/7; census-boundary note present.
4. Spot-check one corpus claim per spec you haven't seen verified before.
5. No collateral edits beyond prescribed scope (diff the three files).

## Deliverable

`docs/research/verifications/ds123-vA.mdx`; final line exactly
`VERDICT: PASS` or `VERDICT: FAIL — <one line>`. ≤12 lines.
