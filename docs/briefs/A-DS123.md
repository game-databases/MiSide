# Brief A-DS123 — Arbiter: build-readiness of the three dataset specs (MiSide)

You are a FRESH Arbiter subagent of the MiSide orchestrator. CANNOT spawn
agents; `git log/diff` allowed; never write commands. You rule; you edit
nothing.

## Case file

1. SPECS: `MiSide/docs/specs/dataset-characters.mdx`,
   `dataset-achievements-endings.mdx`, `dataset-dialogue.mdx`
2. REVIEWS: `docs/research/verifications/ds{1,2,3}-review.mdx`
   (NEEDS_REVISION 1/2/2)
3. FIX VERIFICATION: `docs/research/verifications/ds123-vA.mdx` (PASS)
4. Frozen pack spec (`MiSide/spec.md`) entity sections + FRAMEWORK §8 DoD
   as the bar.

## Mission

Per spec, rule build-readiness:
- Could a builder curate the dataset from these instructions without
  guessing (every field sourced, every join keyed, ACs executable)?
- Are the deferred items (theme→Mita rulings, native-body pass,
  locale_availability dependency) properly fenced as explicit deferrals
  rather than silent gaps?
- Is scope honest vs the DummyDll-stripped-bodies reality?
- Any cross-spec inconsistency (e.g., dialogue speaker attribution charged
  to DS-3 but chars expects it)?

## Deliverable

`MiSide/docs/research/verifications/ds123-arbiter.mdx`; per-spec ruling
lines then exactly one final line:
`RULING: ALL_APPROVED — launch builders` or
`RULING: PARTIAL — <which specs blocked, numbered fixes>`. ≤14 lines.
