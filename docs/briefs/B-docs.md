# Brief B-5 — Dataset Builder: documents & lore collectibles (MiSide)

You are a fresh Dataset Builder subagent of the MiSide orchestrator. You
CANNOT spawn agents. You never run `git` commands. Write ONLY:
`C:\_reps\game-databases\MiSide\extracted\data\documents\`,
`C:\_reps\game-databases\MiSide\contracts\dataset-documents.mdx`, and your
build-log block. Corpus read-only at
`C:\_reps\game-databases\MiSide\extracted\`; game root NEVER written.

## Read

1. CONTRACT: `docs/specs/dataset-documents.mdx` — arbiter-approved
   (`verifications/ds456-arbiter.mdx`) post-F-DS5: AC-1 = exactly 14 rows
   (#0–13), 21-equality delegated BY REFERENCE to DS-4 AC-2, row 0 =
   `mta`/level17/FlashTaker-placed. Build EXACTLY it.
2. Fence obligation (arbiter): your build-log block records that emit-stage
   PIPE manifest registration carries the DS-5 §1.2 consume-by-reference
   citation + §9 stage ordering after DS-4's, same-commit.
3. Placement facts are DS-4's — you CONSUME by reference; never duplicate
   placement rows (identity joins to DS-1 J5 remain yours).

## Mission

Curate per spec (profile_document ×14 + world_document ×160 under the
dedupe rule + BlackRoom parts + book textures); note-content carrier stays
stub/deferred per R1 — no invented prose anywhere; emit contracts doc;
self-check all repo-executable ACs; byte-deterministic outputs. Append
"B-5 build" block to `docs/research/build-log.mdx`.

Final message ≤8 lines.
