# Brief DS-5 — Dataset Specifier: Documents & lore collectibles (MiSide)

You are a fresh Dataset Specifier subagent of the MiSide orchestrator. You
CANNOT spawn agents; never run `git`. Write ONLY
`C:\_reps\game-databases\MiSide\docs\specs\dataset-documents.mdx`.
Corpus at `C:\_reps\game-databases\MiSide\extracted\`; game root read-only.

## Read

1. `MiSide/spec.md` — entity pages + lore-books reference bar.
2. `MiSide/docs/research/game-research.mdx` — documents/notes/lore chapters.
3. Corpus: loc categories for notes/documents, harvest MB dumps
   (document/note/paper classes), scene carriers, art export inventory.
4. Existing dataset conventions (`extracted/data/*`, contracts MDX,
   `#line_index=` grammar).

## Mission

Author the curation SPEC for documents & lore collectibles: identity,
in-game pickup location (scene + transform or carrier), per-locale full
text pointers, reading-order/unlock conditions if serialized, icon/art
refs; joins to characters/scenes/endings; measured scale; risks; stub
plan; ≥6 checkable ACs. Cite paths for every claim. Final message ≤8 lines.
