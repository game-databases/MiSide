# Brief B-4 — Dataset Builder: cartridges & mini-games (MiSide)

You are a fresh Dataset Builder subagent of the MiSide orchestrator. You
CANNOT spawn agents. You never run `git` commands. Write ONLY:
`C:\_reps\game-databases\MiSide\extracted\data\cartridges\`,
`C:\_reps\game-databases\MiSide\contracts\dataset-cartridges.mdx`, and your
build-log block. Corpus read-only at
`C:\_reps\game-databases\MiSide\extracted\`; game root NEVER written.

## Read

1. CONTRACT: `docs/specs/dataset-cartridges.mdx` — arbiter-approved
   (`verifications/ds456-arbiter.mdx`). Build EXACTLY it: 23-key registry,
   21 FlashTaker placements (DS-4 = SINGLE placement authority — you own
   all 21 `(container, save)` rows incl. the 10 player-side), AC-7's
   measured hash-partition assertion, TV −1 loc offset pin, community-name
   firewall ("Hetoor" appears nowhere).
2. Fence obligation (arbiter): your build-log block must record that this
   dataset's emit-stage PIPE manifest registration carries the DS-4 §1
   restatement to the 11-row-overlap scope in the same commit.

## Mission

Curate per spec (cartridge_item + minigame rows, 17 owned minigame rows);
emit contracts doc; self-check every repo-executable AC; byte-deterministic
outputs (sorted keys, stable order). Append "B-4 build" block to
`docs/research/build-log.mdx`: counts, join coverage, AC scoreboard,
deviations (none expected).

Final message ≤8 lines.
