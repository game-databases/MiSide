# Brief F-SD1 — SpecFixer: apply R-S-D1 findings to MiSide spec.md

You are a fresh Fixer subagent launched by the MiSide orchestrator. You
CANNOT spawn other agents. You never run `git` commands. Touch ONLY:
`C:\_reps\game-databases\MiSide\spec.md` AND, for finding 2 only, ONE line
in `C:\_reps\game-databases\_foundation\decision-register.md`.

## Read first

1. `C:\_reps\game-databases\MiSide\docs\research\verifications\s-d1-review.mdx`
   — APPROVE with 3 minors + 2 nits. Apply ALL five exactly as prescribed.
2. The spec itself.

## Fix directives

1. **UGC section-map row:** add the missing section-map row for user
   screenshot uploads ([DR-2026-08-19-ugc-accounts] item 3), tagged per
   site-sections bar, cited like its sibling rows.
2. **DR register amendment:** in `_foundation/decision-register.md`, entry
   [DR-2026-08-24-miside-pack] ¶4 — replace the stale "30 store-listed"
   figure with the measured **31** (one-line edit; do not touch any other
   DR entry; preserve heading text exactly).
3. **Owner gate:** finding 3 is NOT fixable by you — ensure the spec's
   pre-freeze gaps list explicitly carries "tier/domain = owner-only D3"
   as a hard freeze gate if not already there; no other action.
4. **Nits:** "entity kinds of pages" → "entity pages"; correct the EN wiki
   size to the disclosed non-redirect figure (99) with raw count kept as
   context, per the source citation.

## Rules

- Keep every citation and table row otherwise untouched. No renumbering of
  sections. Final message ≤6 lines: per-finding before→after summary.
