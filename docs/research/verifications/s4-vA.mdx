# v-s4a — Verifier A on I-S4 (re-executed the evidence)

Re-ran the pinned argv (`<container> -m dump -t monoBehaviour --assembly-folder extracted\il2cpp\DummyDll --export-asset-list xml -o <out>`, spec §2 S4 shape) against the 51-container sandbox copy in fresh dirs `work\v-s4a-check{1,2,3}-*`. **(1) Patched 0.19.0.1** (that exe's FileVersion) × `level1`: exit 0 / 00000000, **1542** typed dumps + 1 xml, exactly 1 "Recursive serializable type" warning, 3.0 s — spot-read `ConsoleEditor.txt` carries real fields past the 9-line header (`fullInfo`, `objectsConsoleDeactive[]`), so not R1's degraded mode. **(2) Stock 0.19.0.0**, byte-identical argv, same container copy: exit −1073741571 = **C00000FD** STATUS_STACK_OVERFLOW (unsigned 3221225725), dead after 51 txt — crash-point variance again matches parallel-export ordering; sole changed variable is the binary ⇒ causality proven, not coincidence. **(3) Sweep spot-check** on the census-median container `sharedassets17.assets` (median of sweep-matrix.jsonl's 51 rows): patched exit 0, **2357** txt = its census row exactly, 1 xml, 1 warning, 0 failures. While picking the median row the sweep total re-reconciled: 51 rows sum to 140,329 dumps as claimed.
VERDICT: PASS
