# R-FVC2 — Code review of F-VC2 (commit 665fe74d, 23 files, all under `site/`)

Re-execution, not claims: suites re-run in `site/` = tests **29/29** · hex lint OK · parity OK ×34 · `tsc` 0. Live leg on my own `next start -p 4941` + stealth Chrome; server killed port-scoped (PID 46852), scratch removed.
1. **Display-name layer [highest risk]** — VERIFIED: 22/22 anchored cartridges join depicts/contains → personages `name_loc`; only documented `mta` de-slugs; `deslug()` re-spaces shipped strings only (rule 8 safe); endings art rides contract column `achievement_id`. Consumers h1/title/breadcrumb/search/OG confirmed; API keeps raw `id`+`url` per ruling. All-34-index sweep 5,168 rows: zero raw-id titles, zero double-NS.
2. **Hover corruption** — VERIFIED: old `group-hover:` self-trigger is structurally dead (descendant selector can't match its own ancestor); now `hover:` on the Link itself + data-driven flags (`present_but_unreachable`, mode-stub).
3. **Search cap** — VERIFIED: new tests pin behavior (dense/sparse fixture fails under old hard-20); live "mita" over my served idx = **54 visible / 8 kinds**, no chip.
4. **Locations art** — VERIFIED: webp traces name-for-name to `globalgamemanagers/Door Wooden 1.png`; all 5 indexes ≥1 img live (11/1/1/1/3); public/img 13 MB ≪ 200 MB.
5. **Motion/titles/rail** — VERIFIED live: dialogue `getAnimations()`=2 (`ms-scan-breathe`), SPACE keycap mounts with band (Radix portal), 404 drifts, home rail renders node counts; RU dialogue title locale-resolved.
Findings: NIT — keycap invisible in static HTML (portal-mounted); state evidence method next time. NIT — stat chips re-expose `save_key`/`flash_save_key` as machine voice (ids are data; consider labels so they never read as titles). No out-of-scope changes; bonus fix of latent `chapter_name_en`→`chapter_name_loc` read noted.

VERDICT: APPROVE
