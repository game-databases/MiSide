# DS-2 spec review — dataset-achievements-endings (2026-08-25)

Re-derived independently against pack-local `extracted/` (brief's `D:\…\work\extracted` path is stale). CONFIRMED: store table 26/26 byte-matches §A.2 incl. icon pathIDs, lineTranslate, `get=True` only @[0]/[3]; EN names verbatim + 34 locales × 26 lines; all 13 award cites opened — target pathIDs/types/methods/string args byte-match (11 `AchievementGet` + 2 `ClothCompleted`); `Events_Data_#6042` = 9 ordered calls ending `AchievementGet("ACHI_none")`; own-grep counts reproduce: DialogueChanger 13 (exact 10-level spread), IntMemory 5 (shapes 2/2/2/4 + sibling 1×7), Events_Data 379; all 10 dump.cs line cites + tokens 0x60000F2/F3, 0x60005F5; `'4970'` literal; Basement_Safe ×2 + consoles; MenuEnding_#2903 (GO 515, textSafeDoor→3492); Location4Condition in exactly levels 3–6; icons 0/26 exported (§A.5.2 premise true); Peaceful strings @ Menu line_index 130/147. Four-class taxonomy honestly applied; no field claims proven behavior.
- **F1 · MODERATE · blocks**: B.1/AC-B2 ObjectInteractive census mislabels composition — truth: **353 pure `ObjectInteractive`, all 353 wired**; the "391" folds in 38 non-subclass siblings (ReqIK 26, ItemTake 11, Group 1 — separate MonoBehaviours; ReqIK has no `eventClick`); CaseInfo 58 excluded separately. "Unwired interactives = 38" is false (zero exist). Pinned wired set (353) still reproduces byte-for-byte; rewrite the census sentence + AC-B2 excluded-census rows before emit.
- **F2 · MINOR · blocks**: AC-B1/B.1 cite `Menu.jsonl:130`/`:147` means loc **line_index**, but the colon form parses as FILE line (= index 129/146: empty string + epilepsy warning) — an AC-B4 lint passes it yet binds the wrong record. Loc grammar must pin `line_index=` explicitly.
- F3 minor: "MenuEnding" TimelineAsset collision is 45 same-name dumps, not "~20" (direction holds; AC-B7 pins fine). · F4 nit: B.1 gloss "DestroyGameobject/SetActive ×2" compresses 5 mid-chain calls (actual D,S,S,D,D); AC-B8's ordered-list bar is itself correct.
Everything else survived adversarial contact; F1/F2 are text-only fixes, no data-model change needed.
VERDICT: NEEDS_REVISION — 2
