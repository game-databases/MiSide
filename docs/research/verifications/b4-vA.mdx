# B-4 verifier A — evidence re-execution (2026-08-25)
Re-executed with own read-only scripts vs corpus (not the builder's harness): **AC-1** literal @`0x13AD860`[4223] split == 23 save_keys byte-exact in order, 13 char/10 player by slot; **AC-2** pickup multiset == fresh FlashTaker grep (21 inst / 12 containers), unresolved exactly mtad2/mtacore w/ R3 fields, 21 named-file greps clean; **AC-3** J1 22f+22i reuse C13 anchors verbatim, inverses mirror, mta anchorless documented, depicts/contains ids exist in DS-1.
**AC-4** −1 pin resolves lines 0/1 in 34/34 locales (EN exact "Dairy Scandal"/"Penguin Piles"), identity offset OOR ×34; **AC-5** automate ×3 + TV entries (idx 1/2, counters 0) + 12 carrier rows == fresh dump/asset-list greps; **AC-6** 10f+10i ⊆ C11, 8 type_tag ids once fwd+inv, hard/logic labeled, write scope data/cartridges only; **AC-7** 48 controller dumps → md5 groups [19,16,12,1], level2 group = 12; **AC-8** all 24 rule_evidence locators resolve verbatim, scoring_derivable false ×17, prose numbers absent; **AC-9** Arabic MakeManeken 34-vs-35 reproduced from files, ledger-reconciled 34×4; **AC-10** log↔detect pins agree (19029065 / 0.93L), stamps on all 42 rows + metas + relink metas.
Spot-checks field-by-field — `mtacap`/`mtaghost`/`plr1099`: dump line, C1 slot, C13 anchor target, DS-1 id, levelN−2 binding, stamps OK; `Fight`/`Pinguin`: dialogue arrays measured 4/4/0 and 2/2/2 exactly as row claims, name_loc 0/1 under the pin.
New claims verified: 23/23 C1 keys form the exact `OpenPersonage` m_StringArgument set over `level2/ButtonMouseClick_*` (no missing, no extra); `level6/Dialogue_3DText_#7008.txt` dual-grants `AchievementGet("ACHI_PinguinTusim")` + `ClothCompleted("Chirfns")` in one chain, J5 `unlock_site` locator correct, Clothes L12=Christmas; Metroidvania_\* = 240 dumps / 5 md5 ×48 containers, `a4c8a17f…` exactly ×48.
Byte-determinism (static route — emitter has no argv/out-tree override): no time/random/uuid imports, writes `sort_keys=True`, output-feeding enumerations sorted or order-independent (`os.listdir` emit_cartridges.py:193 feeds counts only), written-manifest counts == disk (23/17/2 · J1 44 · J3 20 · J4 76 · J5 4).
Minor defect (non-AC, zero data impact): `_meta.slug_rule` example "`mtacap -> mta-cap`" contradicts its own letter↔digit-boundary rule — emitted id is `"mtacap"` (cartridges.jsonl line 6); build-log Deviation 4 repeats it. Ids verified reproducible+injective via emitter's own `kebab_save`; fix the two prose strings at next touch.
VERDICT: PASS
