# D-S1 vA — verification of F-DS1B's five fixes to site-scaffold.mdx (2026-08-25)

**M1 PASS** — §7 now carries the total source×space table: `inline`×`world-assumed` alone PROJECTS (today only `World.positionSpawn`), and all five non-projecting classes (`inline`-`unknown` incl. the Player_Teleport IL-stub reason, `parent-local`, `object-local-offset`, `pptr-unresolved`, `none`) state pending-placement dispositions with reasons — "no cell of the axis is unhandled"; AC-S7 asserts the conjunction `source=="inline" AND space=="world-assumed"` over the full table, not the source field alone.
**M2 PASS** — §5.1 covers exactly the nine kinds spec.md §Page inventory adds beyond the seven routed contracts: `monster_anomaly`+`chapter` route RESERVED gated on owning contracts (slug-source pinned, zero pages until they land), and `save_point`/`game_version`/`outfit`/`travel_gate`/`secret_easter_egg`/`debug_command`/`game_mode` carry explicit no-page rulings (thin-page ban / glossary-hosted / module-on-parent / tool-hosted / reopen clause) — none silent; the machine-plane paragraph bans dangling `/api/v1/{kind}/…` stranding.
**S14+§10 PASS** — §10.1 pins partition == generated static routes × ledger availability with the zero-non-200 invariant (curl sweep on `next start`, orphan `<loc>` = defect on AC-S3's bar) and `lastmod`=buildId; §10.2 assigns JSON-LD ownership per piece (Organization/WebSite/VideoGame graph in root layout, BreadcrumbList on breadcrumbs, entity `Thing` via the EntityShell slot, ItemList/HowTo/NewsArticle/SoftwareApplication to their pieces); AC **S14** row + §2 tree pointer landed.
**Advisories PASS** — §3.3 records why hand-rolled keyed chrome JSON beats §2.20's next-intl/next-translate (≤150 flat keys ×34, plain key-set-diff parity, pivot-bare two-tree must not grow middleware, §2.4 mechanism clause satisfied verbatim); §4.2 label fixed to "all 14 physical T2 §6 rows" with the kinds-vs-rows note on T2's trailing "12", and AC-S8 diffs the full table.
**Scope PASS** — the fixer's edit is confined to `docs/specs/site-scaffold.mdx` (+128/−13, commit 512d0773; the commit's other file is the orchestrator's own launch brief; current working-tree deltas belong to sibling agents, not this fix).
VERDICT: PASS
