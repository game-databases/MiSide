# ds123-vA — verifier, three dataset-spec fix rounds vs ds{1,2,3}-review

Diffed `be04bf2..25d46a6`: only the three specs (+ orchestrator's own brief); tree clean — no collateral edits.
- **chars:** S13/J2 now cite `Location11_BlackMita`/`Location11_EyesMitaBlack` joined via transform T (both ×1 in level13.xml; bare `MitaBlack` `<Name>` in zero asset-lists — substring only inside EyesMitaBlack; `MitaCore` verbatim in exactly level15+level17); AC-7 asserts T-products and zero raw `resource_path` Names; `preview_prefab_key` `[unverified]`; locale_availability annotated planned PIPE P1 output.
- **ach-end:** census = 353 pure `ObjectInteractive`, all wired (swept all 353 dumps: zero empty `eventClick`); exclusions pinned ReqIK×26 / ItemTake×11 / Group×1 (compose the false "391"; ReqIK has no `eventClick`) + CaseInfo×58; `#line_index=` everywhere — the sole bare colon form left is B.3's own counter-example; collisions **45** (46 filename hits, real component `level2/MenuEnding_#2903.txt`, all other 45 carry `m_Tracks`); 9-call chain named, mid-order D,S,S,D,D byte-verified (GO #20/#166/#170, SetActive(True)×2, `ACHI_none` last).
- **dialogue:** §3.6 restated as union-of-carriers with failures level3 91<143/Mob, level16 207<217/indexFile, level20 216<259/group indices, span levels 16/17/19/20 added (reproduced: LD1=143, L3 max 91, Mob max 143); `line_index = indexString − 1` contract at §3.2 comment + block, §3.6, AC D2 with can't-catch-±1 rationale; fixer's disclosed deviation verified true (`#7124` indexString=1 → LD4 idx0 `"Mita?"`); blanks 1,101 measured, breakdown 3,017+328+1,101=4,446 byte-exact; cc@58 = 6/7 per residue ledger (Serbian (Latin) no-cc).
- **Fresh spot-checks:** chars S14 census MitaPerson 45 / MitaKiller 31 / Freak Enter 17 / ChibiMita 3 — exact; ach-end `ACHI_final` award site `level18/ObjectAnimationPlayer_#1041` confirmed; boundary numbers reconcile as out-of-scope deltas (UIText 1,780+1,575=3,355; Events_Data 279+100=379).
- Nit (non-blocking): boundary note attributes those rows to "`sharedassets*`" while 82 UIText + 8 Events_Data sit in globalgamemanagers/resources.assets — wording inherited verbatim from the review's own LOW finding; the numbers are corpus-exact and the prescription (out-of-census-scope, never data loss) is met.

VERDICT: PASS
