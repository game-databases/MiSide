# s6-arbiter — Arbiter on A-S6: I-3 loc-encoding policy + I-4 ilspycmd errata

1. **I-3 — option b strict ADOPTED.** The math closes without touching AC-9: the 7 ledgered files are exactly the 2,210−2,203 shortfall, so recover-or-mark emits **2,210/2,210**, while (a) deadlocks every full run under AC-3 forever and (c) must amend the floor to bless a permanent hole in the 34×20 LocationDialogue matrix — both violate Principle zero where the data is provably reachable (75 Slovak/Serbian segments round-trip-decoded losslessly, orthography-verified). Honesty holds: proven bytes decode; the five single-stray-byte files' `0xCC` bytes become **declared U+FFFD** — the same glyph healthy locales already ship verbatim at line 58 (BG/IT/ES literal U+FFFD ×3) — whereas decoding them would fabricate a character the corpus cannot justify; per-file residue rows (locale/category/codec/per-segment offset+hex+recovered-or-FFFD) keep it PROVEN-not-claimed and seed AC-12's residue ledger. §2 S5 amendment ruled: legacy-segment anomalies under the strict rule (ONE declared codec per file, applied only if ALL invalid segments round-trip byte-exactly, else U+FFFD per segment + residue row) exit **0** when every anomaly is recovered-or-explicitly-marked; exit 3 remains for absent store, post-recovery control characters / structural divergence (e.g. line-count off the 71-line norm), and any unhandled anomaly class; add the mechanical invariant `emitted == walked-txt-count` to the stage report so S8 reconciles by construction; ledger writes keep the deterministic-full-rewrite mode (AC-5). EN's reference set regains the category, so the 27 phantom `extra_vs_reference` rows vanish automatically. Run B's `--from art-export --to census` slice is sanctioned in hindsight (entrypoint flags, idempotent stages, zero code touched) and becomes moot under the new exit policy. The 36 UTF-8-BOM files stay byte-verbatim — no normalization on an unverified "likely": known-defects note now, engine-parity decision only after the post-S7 `GlobalLanguage` probe.
2. **I-4 — errata APPROVED; DOTNET_ROOT moves into code.** Spec §2 S7 + toolchain.md §3 stop naming the GUI selfcontained zip as CLI source (upstream v11.0-rc assets are GUI-only; no CLI asset ever existed on that channel — root cause confirmed). Correct pin, version retained: nuget dotnet-tool **`ilspycmd` 11.0.0.9335-rc**, net10.0-any TFM, acquired locally per I-T1 (SDK 10.0.400 extracted under workroot, `--tool-path` install, self-stored); EXTRACTION-LOG pins block gains channel + artifact hashes and flips `verified:false → true` on the proof run. Wrapper option (b) is REQUIRED, not preferred: the S7 stage injects `DOTNET_ROOT=<workroot>\it1-dotnet10` into its child env — option (a)'s caller-exported variable is tribal knowledge against [DR-2026-08-18-pipeline]'s "no tribal knowledge" bar and breaks fresh-clone repro plus AC-16's identical-results-from-both-shims guarantee. Host-global ilspycmd 8.2.0.7535 substitution stays refused (pin integrity). Fix-time riders: retire `_ilspycmd()`'s zip-unzip fallback or glob the exact filename (that channel never contained a CLI); pin ONE canonical dir (`work\tools\ILSpy\` — the report's IlSpyCmd/ILSpy spelling split must not survive into the log); sync the F4 AssetStudioModCLI 0.19.0.1 pin lag in the same commit (same-commit rule). F5 audio-constant and F6 catalogue-aggregation corrections ride the docs commit, non-blocking.
3. **No blocker before X-3 resumes S7→S8.** S7 inputs exist (57 DummyDlls from wave-3 S3; tool proven: exit 0, 115 .cs, structure markers, flags match spec shape) and free space is ample (D: ≈313 GB vs the 10 GB bar). Binding order: land the code+errata commit → rerun S5 under the ruled policy → `run_all <game-root> --from decompile --to census` → S8/PROOF.md reconciles 2,210 and the unpolluted delta ledger. The `GlobalLanguage` encoding-behavior probe (prediction: engine-side replacement, i.e. in-game already shows U+FFFD there) and the BOM/StreamReader parity check run AFTER S7 from the decompiled tree — recorded evidence, never gates.

RULING: APPROVED — implement both, then resume X-3
