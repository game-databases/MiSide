# T-P1 review — tools-plan.md (2026-08-24)

Rubric declared (P/D/X/C/S, 1–5, equal weight, tie-breaks stated) and applied cleanly: all seven totals re-add exactly; both ties resolve by the declared rule (20-tie → explorer P4 beats console P2; 19-group and 18-tie likewise P-descending). My independent re-scores of Dev-Console Reference and Collection Trackers match within ≤1 point on every axis (console S arguable 4; no drift >1 anywhere). Evidence linkage spot-checked ~25 citations against D1/COMP/T2/E1 — dump.cs 288,102 ln / 6,281 types, stringliteral 619,523 B, ConsoleInterface TypeDefIndex 1441, safe windows 17%/98%/empty, `Cap`→`Cappie` / `Open Dialogue`→`Be Candid`, 11.9%/13.8%/10.5% Steam %, and every cited T2 hex (`#c02dbb`, `#08cb05`, `#fc0f43`, `#b338b1→#d682b1`) verify verbatim; zero fabrication found, and the plan correctly supersedes COMP J8's era-stale "31 locales" with the spec's 34. Stubs are buildable without fabricated rows (console refuses guessed effects, map keeps coordinate markers visible-locked, browser refuses invented graphs); soul sketches bind motifs to semantics (corruption hover = compromised path, CRT green = machine truth, checkerboard void = unmapped rooms), not decoration; scope stays inside extracted artifacts and already-gated owner calls (accounts DR, demo-install left as spec gap 5). Findings:
1. MED §2/§3.3 — tool-discovery process step 1 (site-sections §Tools) mandates a gaming.tools / SERP "calculator|tracker|map|planner" inventory table with traffic estimates in tools-plan.md; none is present, so the map's "no Map-Genie-class incumbent anywhere" (its X5, justifying the pack's heaviest build at C2) rests on COMP's database-site negative findings alone — add the sweep or scope the claim to it.
2. LOW §4 — "exists nowhere in COMP S1–S5" overstates the trio: the console command table itself lives in S1/S5 patch notes (J14 says so) and the ending facts exist as S1 prose; the rubric's own word "modeled nowhere" — which the X4/X5 scores honor — is the honest phrasing.
3. LOW §5 — "ordered by data readiness" is not monotone in D (explorer D3 precedes revealer D4; map D2 precedes dialogue D3); stub-first shipability today is the operative key — state that as the sort key.
4. LOW §3.4 — feeds label TamagotchiGame_Cartridge* / profile sets "readiness on-disk"; spec marks cartridge_item and profile_document readiness unverified (classes known via dump.cs E1 step 7, objects not yet census-seen). Tracker D5 stands on the verified achievements spine alone.
VERDICT: APPROVE
