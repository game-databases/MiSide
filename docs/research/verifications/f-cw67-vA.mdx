# v-fcw67 — Verifier: F-CW6 + F-CW7 vs s6-arbiter (2026-08-25)

**F-CW6** — `DECLARED_CODECS` = exactly s5-legacy-encoding §2's proven pair (Slovak + Serbian (Latin) `LocationDialogue Location12.txt` → cp1250); the five 0xCC locales deliberately unmapped with corpus rationale in-code. Gate probe (live module): clean segments recover losslessly; one poisoned segment ⇒ `proven=False` ⇒ whole file declared-U+FFFD — strict per-file, real. Residue rows carry id `I-3:<locale>/<category>` + per-segment offset/hex/line_index/recovered-or-FFFD → `_ledger/encoding-residue.jsonl` + stage report. Exits per ruling: recover-or-mark→0; absent store / post-recovery control-chars / structural divergence / io-unhandled / invariant breach → StageFailure → EXIT_STAGE=3 (`run_all.py:22`, pinned rc==0/rc==3 by tests). `emitted==walked-txt-count` asserted **in-code** (breach anomaly class fails the stage), not merely reported; ref-set derives from EMITTED rows post-policy.
**F-CW7** — `_dotnet_root` resolves `<workroot>\it1-dotnet10`, `_child_env` injects DOTNET_ROOT into every spawn via `common.run_argv(env=)` (version probe + batch) — zero caller-env dependence on the success path; resolver probes only `workroot\tools\{ILSpy,IlSpyCmd}` so host-global is never reached, and `_version` refuses measured ≠ 11.0.0.9335 naming host-global substitution; no zip/unzip path survives (absence fails with INSTALL_INSTRUCTION); upsert carries channel + nupkg sha256 (byte-equal to errata hash) + artifact=NUPKG_NAME + canonical `ILSpy` toolDir + verified:true, upsert-stable on rerun.
**Cross-check** — F-CW8's spec line (pipeline-run_all.mdx:15) states the same contract as code verbatim; F-TW4's diff is exactly one hunk replacing the old rc≠0 pin with the 3 ruling-conformant tests (12 passed file-tier, 68+4 skipped claimed).
**Smoke** — py_compile ×4 OK; `run_all --list` exit 0 (8 stages).
**Residuals (non-blocking)** — detect.py S2 seed still seeds the GUI-zip artifact name with verified:false until the first post-ruling S7 success heals it (self-healing design per detect.py docstring); spec §2 S5/S7 body prose stays pre-ruling but is superseded by explicit errata-precedence lines (pack convention; AssetStudio §3 lag already flagged to orchestrator); conftest keeps legacy repo-zip fixtures and no test pins zip-retirement/DOTNET_ROOT injection — coverage gap, no contradiction.

VERDICT: PASS
