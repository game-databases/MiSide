# f-cw4-vA — Verifier A on F-CW4 (six directives, commit 9211ab5)

(1) CONFIRMED: `_stage_tool` returns `<workroot>\tools\AssetStudioModCLI-0.19.0.1\` first (CANONICAL_TOOL_DIR_NAME ← TOOL_VERSION_PIN); run() hard-gates parsed FileVersion ≠ 0.19.0.1 with install instructions (the "unknown" no-dll carve-out is disclosed and can't pass a real stock build, whose csproj/FileVersion v-s4b byte-verified); case-insensitive grep for `is4` under pipeline/ = zero hits. (2) CONFIRMED by reasoning both paths: regex `\d{4}\.\d+\.\d+[fpb]\d+` over bytes[:256] leftmost-matches the NUL-padded v22 header's offset-48 token → `2021.3.35f1` (padding holds no earlier ASCII digit-dot run), while a plain v21-style file *leads* with the ASCII version string, matched at offset 0 — no regression; legacy split-at-NUL returned b"" there per v-s4b. (3) CONFIRMED reachable + consistent: abort only under `not ctx.keep_going` (real field, common.py:89); keep-going stages the probe row once and REPLACES it in place via `attempts[probe_staged_idx] = attempt` whether the in-sweep retry succeeds or fails again — one row per container into flush_attempts' deterministic FULL rewrite; budget gains `probe_status`; crosswalk abort gated identically. (4) CONFIRMED derived, not hardcoded: per-row `recursion_warnings` counts R4's verbatim marker line in stdout; the `cyclic-tail-residue` EXTRACTION-LOG event computes len/sum over those ledger rows; the census RESIDUE_SEEDS entry renders into §3 Residue ledger (AC-12) and defers per-run counts to the event. (5) CONFIRMED single + scoped: exactly one hunk (+2 lines) adds the "Errata 2026-08-24" block citing s4-crash-investigation.mdx (6b66ec7 cycle-guarded rebuild, E1 typetree correction, offset-48 note, AC-12 pointer); commit stat and a clean scoped worktree show nothing else in the frozen spec moved. (6) CONFIRMED verbatim vs v-s4b's two flagged facts: sweep row now "45 exactly one each, level15 and level17 two each" (45+2=47) and the r4 caveat records txt:0/xml:0 as delta-counts of the pinned rerun while the dir holds 1542 txt + 1 xml. Smoke: py_compile ×3 clean; `--list` exit 0. Non-blocking minors: `_stage_tool`'s last-resort zip branch does `_find_exe(...).parent` unguarded (AttributeError, not StageFailure, on a degenerate exe-less zip); the regex needs a 4-digit year (pre-2018 Unity tokens unparseable — moot for this pinned install); suite sits at 65/66 awaiting the intentional seed-pin contract fix (T-W2 scope, outside F-CW4's touch zone).
VERDICT: PASS
