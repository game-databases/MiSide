# c-w1-r4 — fresh LIGHT re-review of F-CW3's two fixes

Fix is UNCOMMITTED working-tree state (no F-CW3 commit exists yet; last pipeline commit is c3feec6) — diffed both named files vs HEAD. Site 1 `decompile.py:221-245`: wipe→parse_dump_cs→3×write_json→README inside one try mirroring the batch's exact discipline (:207-211); abort report NAMES the leg (`"structure-graph leg: %s"` via `flush_aborted` :182, which rewrites `status:"aborted"` before propagating); bare `raise` preserves type/traceback; nothing swallowed (no pass/return); batch hunk is re-indent only, behavior untouched. Site 2 `test_census_reconcile.py:83`: each entry pinned by its ID marker `[R-E1-N]`, matched against the single rendered residue line (census.py:239 `- **title** [rid] — text`), needles traced to each seed's own title/body in RESIDUE_SEEDS (census.py:30-45) under all()-semantics — wrong output trips it three ways: seed dropped → `assert rows`; title/body gutted or reworded → needle assert fires (e.g. losing "curation-pass", "dataachievements", "locale-delta.jsonl"); rid renamed → row lookup empty; old `"gi"`/`"get"` probes confirmed tautological (substrings of "logic"/"budget", any() over the whole lowered file). Style matches the neighbor tests' splitlines/comprehension/f-string idiom; census file count unchanged at 7.
Smoke: py_compile clean ×2 · `pytest pipeline/tests -k "census or decompile"` → **11 passed, 59 deselected** in 62s (full offline sandbox runs) · `run_all.py --bogus-flag` → exit 2, matching the documented usage-error contract.
Residual (non-blocking, outside this fix's claim): the anchor-scan/update_defaults stretch (:247-264) stays outside any guard — no realistic thrower identified; this fix closes r3's R1 exactly as claimed.
VERDICT: APPROVE
