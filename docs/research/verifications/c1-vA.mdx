# CLOSURE-1 verification — Verifier A (evidence lens)

Recounted `extracted/relinks/` independently: **25 relation files / 1,159 edges / 25 `_meta` anchors** exact; all `_meta.row_count` agree; provenance 30 rows, every `canonical_file` exists. Sampled 3 files — character--cartridge (44 rows = 22 pairs, both directions + semantic agreement vs cartridges `depicts`/`contains`, `mta`/mita-usual/mita-true excluded per declared fence), document--achievement 28/28, scene--dialogue-pool 40/40 — **every edge resolves against the datasets**. locale_availability recounts to **2,686** = 680 dialogue + 1,734 category-presence + 272 book-page; all 680 dialogue cells match `availability.csv` field-for-field (csv `''`→JSON `null` on filler tail_delta is benign); sampled cells of every kind ground-truth on disk (ACH 26=26, CHAR 13/165/47/26, CART 8/34, SCN 6/6, book art, L17 near-absence correctly measured). Determinism: assembly script removed → static route: metas recursively sorted-key, data rows byte-verbatim vs parked sources (4 files seq-identical), sha-stability claim not re-runnable but consistent/uncontradicted. missingdata.md = **83 entries exactly** (12+60+3+8); 7 entries traced to real marks (CH-8, CAR-9 34-vs-35, MA-2 fr-L16 0 B, DLG-6 =12 ledger rows, XC-5 markers `_meta`-only, SCN-2/DOCX-2 poi census 298=262+13+23 · 76=25+19+18+14 · none 612, END-5 zero feeds) incl. spec anchor §9-R3 at `dataset-scenes.mdx:410`; no invented gaps, classes sensible. PROOF §5 appended over intact §1–4; its join table sums 1159 and locale/entity numbers reproduce.
VERDICT: PASS
