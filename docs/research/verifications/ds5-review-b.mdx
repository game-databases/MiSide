**R-DS5b — Reviewer B (conformance lens, light)** · specs read in full; artifacts spot-checked only.
1. Schema: §4.2–4.4 cover identity (`document_id`/`subject_character_id`/`registry_ref`), placement (`placement{container,path_id,carrier_class}`+mechanism), per-locale pointers (`lore_loc`/`name_loc`, §4.1 GetString ruling), art refs (`sprite_ptr`, `art_per_locale`); every field names a source, no orphans.
2. Cross-spec vs DS-4: no NEW conflict — both pin documents=profile_document content / cartridges=cartridge rows / same settle-toward-C2 tiebreak; DS-5 not yet carrying the consume-by-reference citation matches DS-4 §1's scheduled pre-emit edit + the known arbiter caveat.
3. AC sample 1/2/3/5/8: 2/3/5/8 repo-executable + discriminating (cited artifacts verified on disk); **AC-1 fails**: demands exactly 13 rows while its own §3 table and its multiset target DS-1 §3 both have 14 (#0–13); a Mita-only file holds ≤11 placements so "equals the 21-row census exactly" is unsatisfiable (full-21 equality is DS-4 AC-2's); row 0 records empty key/story-granted although `level17/FlashTaker.txt:10` = `string save = "mta"` and the spec's own finding 1 + "11 placed" sum check require it placed.
4. Risk honesty: R1 handled without smuggling — every note `text_mechanism:"unresolved"`, `text_loc:null` never backfilled, baked-texture hypothesis left unresolved pending R5; AC-5/AC-10 gate invented prose out of data rows.
VERDICT: FAIL — AC-1 unsatisfiable as written: 13-vs-14 row count (own §3 and DS-1 §3 are 14), impossible 21-census equality for a Mita-only file, row 0 drops the measured mta@level17 pickup its own findings assert.
