# b4-vB — B-4 cartridges build, spec-conformance lens (2026-08-25)

All facts re-measured first-hand, no log trusted: C1 literal @0x13AD860 splits to the identical 23-key multiset **in order**, 13 character / 10 player by slot (slot13=`plr1a`); `minigames.jsonl` exactly 17 (3 prefab/scene + 2 TV + 8 carrier + 4 tamagotchi); candidates = 2 honest tier-3 rows, tier-4 ships no row (README-noted). Fresh corpus grep = exactly 21 `FlashTaker*` dumps, `(container, save)` multiset equal to `pickup_ref` value-for-value **incl. all 10 player-side**; unresolved exactly `mtad2`/`mtacore` with R3 named; J2 parked meta-only declaring single authority + DS-5's 11-row-overlap consume-by-reference — no delegation gap. J1 44 edges = 22 fwd + 22 inv, `flashes:<key>` anchors and entity pairs byte-match C13's 22 keys (`mta` honestly anchorless); J3 20 edges, 8/8 minigame-tagged ids exactly once fwd+inv ⊆ C11, award/unlock sites re-grepped (#7008 dual grant, #7081, #13057), 6 null-target partials honest; AC-7 partition re-hashed → [19,16,12,1], level2 md5 `2a12c637…` group=12 matches `_meta`. Contract binds standalone (schemas/types/nullability/pinned slug rule/binding locale model/non-goals); build-log block records counts, join coverage, deviations, and the arbiter FENCE (same-commit PIPE registration + DS-4 §1 11-row restatement). Firewalls hold: Hetoor/Spaceracer grep **zero** in the client corpus (stringliteral + localization, this pass); in the emitted tree they occur only as cited `community_alias` values (the spec §4.2/R8-sanctioned slot — brief's "zero hits in emitted data" read literally would ban the spec's own example; ruled per spec-conformance lens) and **never as keys**; `scoring_derivable` false ×17; D1 prose numbers absent from all data rows; J6 meta-only measured absence (downgrade respected). Minors, non-blocking: applesnake J3 rows carry hard-row boilerplate text in `method` while `award_site=null`/`mechanism=logic`; PumpkinClicker evidence claims `int size = 5` ×2, grep measures ×3 (locator itself resolves).

VERDICT: PASS
