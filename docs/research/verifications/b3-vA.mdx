# B-3 verifier A — dialogue build, evidence re-execution (2026-08-25)

Re-executed D1/D2/D5/D7/D9 on raw artifacts: census exact (2,710 `Dialogue_3DText` filename recount; 13 changers/70 cases parsed; 11 L14; 11 L18; 24 mobs; `Location21_DialogueRandom` = 0; theme dist 14/14 = spec §2.2). Raw baselines reproduce: nextText 2,162 non-null partitioning 2,710 with exactly 548 null-terminals; eventFinish 662 (+39 print, +2 dontVoice); edges close as 2,150 resolved + 11 group-nextDialogue + 12 dangling = 3,776 by kind; graph terminal sets sum 548. Level5 off-by-one proven semantically (exit case stringFile 2 → −1 lands `"Never mind."` in `Location 3`; identity copy hits two `// Quest` rows) and uniformly on L14 forks (emitted line_index = raw slot − 1, branch text pairs coherent). Parity refutation reproduces corpus-wide — exactly Filipino LD20 +1, Italian LD3 +2, Português-Brasil LD18 −3, Turkish LD6 −1, all other 34×20 cells even. Identity recovery closes end-to-end (scene obj 11590 = DialogueChanger on GO 433 ↔ bare dump ↔ emitted node); ledger's empty-host evidence is correct per scene files (mb-dump dirs carry foreign sharedassets pids — e.g. `Image_#18521` ∉ level14 scene — so scene is the right authority). D9 static check: fixed block ordering, zero wall-clock fields anywhere; emitter rerun not executed (its OUT path is hardcoded to the live tree — a rerun would mutate the corpus).

**Decisive defect — D7 mis-scored PASS:** direct presence test shows **11 of the 328 EN author-comment rows ship nowhere** as `condition_hints` (LD1 104/108/119, LD14 75/139/163/180/199, LD19 378, LD7 271/655); emitted hint *items* total 320 vs 328 source rows. Cause is structural (`attach_hints` applies only each node's first hint-bearing ref, plus targets on carrier-less rows) and undisclosed (`unattached_rows: []`, no diff rows); scoreboard cites the attach-stage stat ("328/328 attached verbatim") while AC D7 requires the emitted count to reconcile. Minor: "10 of 20 wired L14 forks" — measured wired slots are 10 on 5 of 11 components, denominator unsupported; B-dialog.log says 30 byte-identical output files vs build-log block "29" (actual: 30).

VERDICT: FAIL — D7 mis-scored PASS: 11 of 328 comment rows absent from emitted condition_hints with no ledger explanation; every headline number otherwise reproduces exactly.
