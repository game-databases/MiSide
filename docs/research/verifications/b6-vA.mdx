# b6-vA — Verifier A: B-6 scenes build, evidence re-execution (2026-08-25)

Independently re-executed from corpus (mb-dump, localization ×34, stringliteral.json, decompiled enum) against `extracted/data/scenes/` — builder's `check_scenes.py` not reused. **S1**: 24 rows (20 story/boot/title/menu/unbound); 20/20 spawn tuples byte-equal dump↔emit↔spec §2.2; exactly one `World`+`WorldPlayer` per story level; level23 `unbound`/null. **S2/S3/S4**: partition exact — 19 loads + 7 unloads + 15 continues = 41, + 15 chapter rows + 1 level18-absence ledger = 57, edge sets tuple-identical to dumps; lattice closure CLOSED; chapter pointers **15** (not §5's projected 16) resolving non-empty in all 34 locale dirs, zero-parts emit null; save vocabulary 19⇄19 both directions, zero residue. **Corrections hold at source**: `mb-dump/level7/Scene_Load.txt` measures unload `"Scene 5 - StartHorror"`; ECR eventDay measures {0=halloween ×3, 1=christmas ×24−21} vs DEC enum `{halloween, christmas, none}`; `LocationHint Location18` = French 0-byte shell, zero locales carry content, EN pools 18 ≠ spec §5's 19. **POI**: dedupe rule reproduces KeyHint 657/382 · Transform_Position 105/71 · ObjectInteractive 353/312 · Trigger_Event 359/334; 986 rows / 23 classes; source split inline 298 / pptr-unresolved 76 / none 612, every row labelled, all inline coordinates real (23 Transform_Position via declared `points[]` superset — 49 points, all finite), pptr targets named kind+pathID (#10938→6192 confirmed), ObjectItem 262/262 `object-local-offset`; markers v0 `_meta`-only with 4 pending families each carrying owner+unblock; FlashTaker consumed **by reference** (`placement_authority` → DS-4 cartridges.jsonl, no duplication), saves 20/21 vs DS-1 gallery with `mta` unmatched + README curation rows (`registered-unresolved-pickup`; vs landed DS-4 registry all 21 now match). **Determinism static**: no wall-clock tokens in any of the five JSONLs, build_id on records (scene-links pins at `_meta` per §3.2's own row shape), orderings as declared. Minor build-log drift, data unaffected: its position-census line claims inline 346 / none 564 while counting all 71 Transform_Position inline — emission's honest 298/612 (empty-array TP → none) matches that same line's own parenthetical; and its ECR multiples list drops level16 ×2 (needed for the 24 total; spec §2.7 had it right).

VERDICT: PASS
