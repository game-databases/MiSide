# b3-vB — B-3 dialogue build vs DS-3 (spec-conformance lens)

- Schema per §3.2: every node column present (id/kind/level/location/chapter/speaker/text_ref/emotion/style/voice_present/condition_hints/build); census 2,710/13/70/11/11/24 matches ASL exactly; `random_router` absence re-verified (0 mentions across asset-lists — deviation #5 honest). Edges 2,161 next + 70 choice + 5+5 branch + 1,535 action = contract table exact; `chapter:null` everywhere (SPEC gap #1 honored).
- Off-by-one reproduced against live LOC at two independent points: level6 game_index 1 → LD4 line 0 `"Mita?"`; level5 exitButton case → `Location 3` line 1 `"Never mind."`. Locale pointers = `text_ref{category,game_index,line_index}` ✓.
- D6 cap is truly structural: 107 = choice_case 70 + quest_box 13 + pet_dialogue 24, none serialize themeDialogue/personage (pet `nameString` is a display pointer, not a speaker mechanism under D6's definition) → 2732/2839 = 96.23% arithmetic-exact; every kind WITH a mechanism carries one. speakers.json covers the full closed enum with exactly 5 `{slug:null,status:"pending-curation"}` (MitaKnow/MitaFon/White/Creepy/MitaDream per fence); node spot-check ships the null verbatim.
- Parity refutation shipped as data: `_ledger/locale-parity.jsonl` = exactly 4 rows, re-measured from disk (93/439/256/149 vs pivot 92/437/259/150); `tail_delta` column present; pivot EN stays range authority; nothing silently normalized; contract states REFUTED and leaves the spec amendment to the specifier.
- Residue join closes end-to-end: residue-links row ↔ all 7 ids exist in the loc encoding-residue ledger (incl. Serbian whole-file cp1250), EN LD12[58] confirmed carries U+FFFD. Minor: no back-pointer column on the node record — "inline" is met via the spec's own §7 residue-links mechanism, not a per-node field.
- Contract binds standalone (shapes, counts, id scheme, joins, scoreboard, deviations, handoff all restated). Parking at `data/dialogue/build/` acceptable: builder write scope excludes `pipeline/stages/`, precedent-consistent, PIPE-registration duty explicit in Handoff.
- Minors, none verdict-bearing: contract cites the boundary proof by dump-filename "level6 #7124" — the id space that same doc demotes (shipped identity is `#10588`, evidence reproduces); range-check.jsonl also holds 3 honest `no-bound-category` issue rows beside the 19 validated level rows ("all validated" wording is loose); D6 honesty lives in contract/scoreboard/build-log/TODO but pack README is stale pre-extraction and the dialogue dir has no README (characters set that precedent).
VERDICT: PASS
