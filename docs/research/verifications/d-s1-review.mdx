# D-S1 review — site-scaffold spec (2026-08-25)

Checks 1–3 PASS: locale twin-tree/pivot-bare/301/404 matches [DR-2026-08-20-locale-urls]; all-34 launch correctly derived from FRAMEWORK §2.4 + spec shipping bar (staging would reopen a settled call), aliases ru-x-prerev→ru / ar-EG→ar declared+gated, hreflang ledger-driven (AC S3 == membership; I re-measured the ledger myself: 34 client-dir keys, 2,686 cells, 2108/297/9, Location19 32/34 zh-Hans/zh-Hant — every cited number checks out); stack pins verbatim vs `graveyardKeeper/site/package.json` (next 16.3.2 / react 19.2.8 / radix ^1.6.7 / shadcn ^4.19.0 / minisearch ^7.2.0 all real there); shadcn contract faithfully executes [DR-2026-08-24-miside-pack] ¶3 — Radix owns behaviour untouched, T2 motifs map to named kit components, hex-conversion pin quoted exactly. Checks 4–5 largely sound: search-in-place is the ruling verbatim (no route, hidden-not-unmounted, one function both sides, no URL owed); map no-orphan + focus grammar + authored imagery correct; row counts match all seven contracts. Three revision findings:

- **MEDIUM — S7/§7 projection predicate is source-only.** Scenes contract's truth axis is source×space; "only `source:"inline"` may be projected" admits `space:"unknown"` inline rows (Player_Teleport — IL-stub-proven frameless) and leaves `parent-local` unhandled; AC S7 tests the wrong predicate. Bind projection to `source:"inline" AND space:"world-assumed"`; everything else renders pending-placement.
- **MEDIUM — routing map not total vs spec.md page inventory.** Nine entity kinds (monster_anomaly, chapter, save_point, game_version, outfit, travel_gate, secret_easter_egg, debug_command, game_mode) have no route row and no stated disposition (module-on-parent / glossary-hosted / map-layer / deferred-until-contract) — one declarative paragraph prevents the next piece inventing routes or dropping kinds.
- **MEDIUM-LOW — sitemap declared but unverified.** `sitemap.ts` per-section-per-locale lastmod=buildId carries no AC; add S14 (partition == generated static routes × locales, zero non-200 targets). Also assign schema.org JSON-LD ownership (Organization/WebSite sitewide + per-type) between scaffold and page pieces.

Advisory (non-blocking): record why chrome i18n hand-rolls keyed JSON instead of §2.20's named next-intl/next-translate (parity-script design already satisfies §2.4's mechanism clause); the "12 T2 §6 rows" label — T2's own table has 14 physical rows, §4.2's enumeration is complete so only the label misleads (relabel to "all T2 §6 rows" for S8's diff target).

VERDICT: NEEDS_REVISION — 3
