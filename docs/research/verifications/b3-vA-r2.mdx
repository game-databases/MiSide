# B-3 verifier A round 2 — F-B3 D7 fix re-execution (2026-08-25)

Independent recount: EN sources hold exactly **328** `//` author-comment rows across the 19 `LocationDialogue` categories; emitted `condition_hints` carry **320 distinct rows as 324 placements over 318 nodes**, every node's actual hints equal my own re-derived expectation (verbatim text, lang "en", 0 mismatches); unattached = exactly LD1·119, LD14·75/139/163/180, LD19·378, LD7·271/655, each in `hint_stats.unattached_rows` with reason + target line; 320+8==328, partition proven by my own set arithmetic — no silent drops. The vA-lost rows now ship: LD1·104 (`// Name Developer`) + LD1·108 (`// Bad name`) on the level3 mob nodes, LD14·199 (`// Continue`) on both L14 carriers. Blast radius git-proven (b29ce9a3 vs 71b5f6cb, the build b3-vA FAILed): among the 30 outputs exactly {nodes.jsonl, `_ledger/build-meta.json`, `_ledger/ac-scoreboard.json`} changed; the nodes delta touches `condition_hints` only (4 records, all other fields byte-equal); the other 27 outputs byte-identical to the prior build. Minors verified against raw serialization: level16 holds 5 of 11 `Location14_Dialogue` components wiring both fork sides (10 non-zero `indexFileRight/Left` slots) and 12 of 22 slots serializing 0 — the restated contract/build-log denominator is measured truth; file count reconciled 29→30 in both docs and matches disk. Emitter aborts on breach (`raise SystemExit` in `attach_hints`, called unconditionally in the emit flow; checker additionally pins total==328). Two non-blocking observations: the emitter-side assert is near-tautological by construction (target∈carried ⇒ attachment), so the real independence lives in the rewritten checker D7 — code-read plus my recount agree with it; D9's printed sha256 covers the scoreboard itself, so the value legitimately rolls whenever that file changes (cosmetic, not data drift).

VERDICT: PASS
