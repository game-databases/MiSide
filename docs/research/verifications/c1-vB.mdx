# c1-vB — CLOSURE-1 conformance + conflict-adjudication lens (2026-08-25)

1. **Conflicts** — provenance rows 26–27 record both; canonical tree holds exactly ONE identity file `character--cartridge.jsonl`, its `_meta` carrying the AUTHORITATIVE ruling that names both superseded mirrors (DS-4 typed depicts/contains, DS-6 placement-augmented), both absent from `extracted/relinks/`; scenes' placement restatement measured exactly **49 lines**, excluded-and-recorded, census staying in `cartridges.jsonl pickup_ref` per DS-4 §1.
2. **Fences** — `_assembly-provenance.jsonl` policy row carries every obligation of the ds456-arbiter agenda-1 fence: PIPE manifest registration in the same commit (DR-2026-08-18-pipeline), DS-4 §1 restated to the 11-row-overlap scope **before either stage runs**, DS-5 §1.2 citation + §9 ordering after DS-4, DS-6 §2.7 dedupe citation; restated in missingdata XC-6 and the closure handoff block.
3. **PROOF §5 arithmetic** — scoreboards match first-hand surfaces (B-1 10/10 bl:44 · A 7+1 with A5 PENDING bl:134 · B1–B8 all PASS bl:139 · D 8 PASS/1 PARTIAL/0 FAIL == `dialogue/_ledger/ac-scoreboard.json` summary read directly · B-4 10/10 bl:371 · B-5 10/10 bl:579 · S 37/37 bl:488); edge sum = 1,159 over 25 files, identical to the §5.2 family split (110/100/792/16/126/15); locale cells 2,686 = 680+1,734+272; provenance = 30 rows; missingdata = 83 entries counted (12+60+3+8); POI census 298+76+612 = 986 matches §5.1 and reconciles the DOCX-2 correction (346−298 = 48 empty point-sets; 564+48 = 612). Relinks tree sha256 **reproduced independently from disk** = recorded `c419071268e3cf4d…`. Prior content: append-only corroborated via the declared write surface, §5.4 references resolving into §3, and matching external citations; no git access permitted for a byte-level prior-state diff, and the sandbox test-suite PROOF copies proved fixture-scale stubs (unusable as snapshots) — no counter-evidence found.
4. **Parked originals** — all 25 canonical relink files verified as replaced `_meta` anchor (authority_ruling / closure_provenance / consolidated_from) + body byte-verbatim against the parked source, row counts equal to provenance (incl. the speakers.json curated_mapping 14→14 JSONL transform); moved-vs-copied decision recorded twice (staging policy row + DOCX-3: copies retained until XC-6 because run_all stages regenerate their staging dirs), consciously superseding the B-2 handoff's "move … at emit-stage registration" wording rather than silently deviating; dialogue graph family plus residue-links.jsonl/speakers.json kept in place per its contract handoff.
5. **Doc defects** — B-6 position-census original line untouched (build-log:479), correction appended inside the closure block's "Corrections recorded at closure (append-only log)"; scenes README degenerate bullet left unedited and logged as DOCX-1 with repair deferred to the next emitter run. No rewritten history found anywhere.

VERDICT: PASS
