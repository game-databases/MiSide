# s4-arbiter — Arbiter on A-S4: spec errata legitimacy + S4 incident closure

1. **ERRATA LEGITIMATE AND MINIMAL — not a toolchain swap.** Chain closed end-to-end: X-1 I-1 crash (C00000FD ×2, identical frames) → I-S4 single-variable matrix (R0 stock dies / R1 degraded rejected / R4 patched green / S6↔R7 byte-parity where no cycle exists) → RCA proven in source (`ConsoleEditor_HierarchyCase` self-reference @ dump.cs:99328; guardless Cecil recursion untouched upstream since 2022, no fix to bump to) → dual verifiers PASS (s4-vA re-executed causality by binary swap alone; s4-vB source-level) → F-CW4 canonicalization (`_stage_tool` canonical-first + run() hard-gates parsed FileVersion ≠ 0.19.0.1, zero `is4` refs) → errata landed +2 lines in commit 9211ab5, nothing else in the frozen spec moved. Provenance is named on its face (aelurum/AssetStudioMod `6b66ec7` + cycle-guard patch + natives-from-stock-zip) — this is spec §8's "stage wins, corrected in the same commit" working as designed.
2. **CAVEAT PERSISTENCE HOLDS for AC-12/PROOF.** The truncation disclosure is machine-derived, not hand-prose: per-row `recursion_warnings` (verbatim marker count from stdout) → sweep-attempts.jsonl → derived `cyclic-tail-residue` EXTRACTION-LOG event → census RESIDUE_SEEDS renders into PROOF.md §3 residue ledger; errata's last sentence pins it to AC-12. vB's "it evaporates" risk is closed by construction — a future warning-free run records zeros honestly instead of losing the entry.
3. **NO PIPELINE BLOCKER for X-2:** detect offset probed against the real install (`2021.3.35f1`, legacy path yields `''`), probe keep-going verified reachable+consistent, offline suite 66/66. The `ilspycmd verified:false` pin is spec-sanctioned §8 build-time work X-2 itself performs, not a blocker.
4. **DEBT PROMOTED TO DEMAND NOW** (rides the X-2 launch window): `pipeline/tests/test_install_smoke.py:118` asserts stale `"0.19.0.0"` — the gated-tier half of the contract update T-W2 already landed offline. Unfixed, the first install-tier smoke false-fails AC-13 against a correct run and poisons the next execution report; same-string sweep of the inert stub fixture at `test_cli_surface.py:148` rides along. One-string TestWriter fix, due before X-2's AC scoreboard is accepted.

RULING: ERRATA_APPROVED — resume real-install execution (X-2)
