# B-2 verifier B — spec-conformance lens (2026-08-25)

Independently re-measured emitted datasets + contracts against [DS-2](../../specs/dataset-achievements-endings.mdx): row counts 26/4/371/1555/5 exact; census **11 serialized-site · 12 code-body · 3 aggregate**, status split holds the A.2 fence byte-for-byte — unverified-behavior on exactly the 15 fenced predicates (IDs match), proven-hard all 11 with sites+cites resolving on disk.
Grammar clean: zero bare `.jsonl:N` loc cites in data or contracts (sole grep hit is the contracts' own ban note); loc cites use `#line_index=`; edge ids unique and structurally joined (every call_index < call_count, options ⊆ parent node); level5's twin `ifInt=0` branches emit `b0`/`b1` ordinals exactly per the declared deviation; IM edges ↔ flag_tables reconcile per branch ordinal.
Honesty holds: Peaceful stub honest (`mode-stub`, `locked-stub`, cites `Menu.jsonl#line_index=130/147` + `dump.cs:206972/206984`, null achievement/chain); windows sit on safe-of-life only as `[community]` + unverified-behavior ('4970' verified at `stringliteral.json:14923`); 3 dead-ref edges kept in-file as `dead-reference` AND ledgered by edge_id (CoreSkip noted absent from il2cpp metadata — never resolved); feeds_ending empty-as-data with its rule ledgered; get-bool quarantine {0,3}/false; descriptions EN-only `official-feed`.
Independent re-checks pass: AC-B3 redone — 850/850 live Assembly-CSharp targets resolve to decompiled `.cs`; my own lint finds 413/413 evidence/instances cites resolvable; award-site relink == unlock.sites multiset; icon pathIDs match A.2 ×26; conditions-met chain == the spec's ordered 9 calls ending `AchievementGet("ACHI_none")`; final chain stored verbatim mid-chain (idx 4 of 13).
Contracts bindable standalone (per-field type/source/nullability, emission rules, pinned censuses). All four named deviations acceptable vs spec intent: family layout + MDX pins follow B-1 precedent / AGENTS rule 11 / arbiter residue (a); window placement is component-owned and no AC pins a row; the 371-node sweep follows AC-B2 over B.7's smaller stub sketch; `dead-reference` and `official_url` are flagged honest additions.
Minors, non-blocking: endings emit-ledger holds **7** rows vs "6" in the contract Files table + build log (extra award-chain-hosts note row); the DC edge-id grammar line omits the unsuffixed `{level}:DC:…` form 10 instances legitimately use (node-id rule implies it); cross-dataset follow-up — `prompt_loc`/`label_line` store raw `DialogueChangerCase.stringFile`, which the later B-3 build proved resolves semantically at N−1; reconcile before frontend renders choice prompts.

VERDICT: PASS
