# A-DS456-arbiter — build-readiness ruling on the wave-2 dataset specs (cartridges / documents / scenes)

All fixes re-verified first-hand on corpus (no log trusted): AC-7 reproduced mechanically — 48 exact-named `MinigamesController.txt` dumps md5 into exactly 4 groups sized **19/16/12/1**; scale **17** now uniform across §1/§3.2/§4 tree; fresh FlashTaker grep = **21** `(container, save)` pairs byte-matching DS-4 §3.1 pair-for-pair; row 0 re-read verbatim — `level17/FlashTaker.txt:10` = `string save = "mta"`; single-placement-authority ruling landed at DS-4 §1 + §6.
- **DS-4 cartridges: APPROVED** — F1/F2 fixed as claimed (partition asserted incl. the 12-member level2 group, no privileged dump); ACs repo-executable; TV −1 pin, Hetoor-zero-grep, namespace honesty stand as reviewed.
- **DS-5 documents: APPROVED** — post-fix AC-1 is coherent with BOTH lenses: reviewer A's evidence lens now gets the row-0 fact carried *with a locator* (stronger than its acknowledged-state PASS; its 10-vs-12 predicate residual stays honestly `[unverified-behavior]`, untouched), reviewer B's conformance lens gets 14 rows (#0–13), mechanism split 11 placed / 2 script / 1 story, and the impossible 21-equality delegated by reference to DS-4 AC-2. No residual tension between the lenses.
- **DS-6 scenes: NEEDS_REVISION (first review; 12 measurement families sampled).** Reproduce cleanly: `nameLocation` 20/20 exhaustive (`levelN ↔ Location(N−2)` hard), positionSpawn floats exact (spot ×4), Scene_Load 19 + level18-absent + 3 lattice rows field-exact, Menu 99–118 incl. all 8 cited names, SaveGame parity 19⇄19 (=15 primaries + 4 secondaries), Trigger_Teleport 14 all level9 with §3.3's pathID 6192 confirmed, hint pools 19+General, level-scoped carriers 15/312/71/382, S9 slot valid (PIPE stages end at S8). Defects:
  - **F1 BLOCKING — §2.2 `rotationSpawn` column is fabricated `0, 0` ×20**: measured values differ on 17 rows (level3 −136.73/5, level8 180/25, level17 118.593/0; only levels 9/15/22 are truly zero) — violates the every-number-measured charter and makes S1 "tuple-for-tuple + byte-equal-to-dumps" unsatisfiable as written (reviewer-b standard). Fix: replace with measured floats (or drop the column; the dumps stay the anchor).
  - **F2 BLOCKING — dependency-load duplication unpinned**: whole-corpus vs level-scene truth runs 31/15 MitaKiller, 353/312 ObjectInteractive, 359/334 Trigger_Event, 105/71 Transform_Position, 657/382 KeyHint (258/160 notes cross-confirms DS-5 R4); DS-6 states no level-scene-owns dedupe rule, so emitter v0 walking `mb-dump/*` double-counts POIs/markers. Add the DS-5 §7-R4-style rule and fix §2.7's false "(every story level + level23)" coverage (MitaKiller absent in levels 3–7 and 18).
  - F3 MINOR — §2.6(b) proof cites #10938 target pathID 5206; the dump measures **6192** (both grep zero in `level9.xml`, so the absence-proof method holds — correct the number). F4 MINOR — §2.3 finding 2 "adds 3 secondary slots" lists four values; state 15 + 4 = 19.
- Agenda 1 ruled: **require the pre-emit edit, not an emit-time check** — DS-5 AC-1 delegates by name to DS-4 §1, whose "the 21 pairs appear in two emit plans … multisets reconcile byte-for-byte" misstates the **11-row overlap** (the 10 player-side pickups are DS-4's alone), and the promised DS-5 §1.2 citation has no enforcement hook until it lands. Fence: DS-4 §1 restated to the 11-row subset scope, DS-5 §1.2 gains the citation, DS-5 §9 orders its stage after DS-4's — all in the same commit as each emit-stage's PIPE manifest registration, before either stage runs.

RULING: PARTIAL — B-6 blocked pending DS-6 fixes F1–F2 (F3–F4 corrected in the same edit pass); B-4/B-5 cleared to launch under the §1-fence above
