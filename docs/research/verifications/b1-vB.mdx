# b1-vB — Verifier B: B-1 characters build (spec-conformance lens)

1. **Schema:** all 15 §4.2 fields present; superset ruled legitimate — `PersonageResource.cs` (re-read this pass) proves `caseBack`/`buttonMenu` are S2 fields AC-3 *requires* as columns (the spec table omitted them); `file_id` documents external-dependency PPtrs (dump shows `m_FileID=2` on player icons); `player-{n}` is §4.2's own example, forced by 9 empty `resourcePath`s. AC-1 reconciled byte-for-byte: 24 tuples + sizes 10@48/14@301 match `MenuPersonage.txt`.
2. **Joins:** J1 scripted ×34 locales → 0 empty cells, 0 U+FFFD; EN/RU/JA eyeballed. J2 clean: level13.xml:3280/:1552 grep the claimed T-products, `<Name>MitaBlack</Name>` in 0 asset-lists (no hard-equality regression), census reproduces exactly (93/45, 31/31, 17/17, 3/3 — Mob_ChibiMita's 35 naive glob hits are 32 `_Animation` siblings, correctly excluded). J3 n−1 pin verified vs Clothes.jsonl, rejected offset named with both failures. J5/J4: 22 mirrored keys each, empty-save-key rows absent as ledgered; anchors verified (achievements[12] = ACHI_cartridgeplayers → "Caught Them All"; `'Personages/'`, `'/Save/Flashes'` literals exist). J6 names-only, `linked_character_ids: []` — R1 fencing held.
3. **Honesty:** `preview_prefab_key` marked [unverified] in `_meta`+contract; reflection targets deferred via `missing_fields`; `gallery_icon` null-honest; R3 Version gap ledgered (README grep passes); all 8 sampled candidate locators resolve at their claimed lines; wiki-only names absent from personages.jsonl (AC-10 negative pass). No invented lore found.
4. **Contract:** bindable without reading the dataset — locale resolution rules, nullability, id-derivation rule, hex recipe, `file_id` semantics, non-goals all pinned. Nits (non-blocking): per-relink-file row counts unstated; achievement `_meta_anchor` field undocumented; tree shows parked relink paths (true today; must flip at relocation).
5. **Relinks deviation: acceptable-with-relocation** — write-scope exclusion declared in log/README/all 5 `_meta`s; relocate to `extracted/relinks/` in the same commit as `run_all` stage registration (spec §9 / DR-2026-08-18-pipeline), updating `contracts/dataset-characters.mdx` + README paths in that commit; nothing consumes the final path yet.

VERDICT: PASS
