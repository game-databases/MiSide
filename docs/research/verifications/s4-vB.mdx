# v-s4b — Verifier B on I-S4 (reasoning + artifact integrity)

RCA CONFIRMED in source: `ConsoleEditor_HierarchyCase` at dump.cs:99328 with self-referential `List<…> casesMe` at :99334 (family TDI 1429–1431); patch diff (src @ upstream 6b66ec7) touches only TypeDefinitionConverter.cs + csproj 0.19.0.0→0.19.0.1 — a real path-scoped `InProgressTypes` visited-set (warn + truncate at re-entry, `finally Remove` keeps DAG reuse), not a blanket catch. E1 correction is GENUINE — E1 wrote "type trees NOT stripped" ([explorer-e1-hands-on.mdx](../explorer-e1-hands-on.mdx)); refuted by R1's 9-line header-only dumps (embedded trees would survive dropping `--assembly-folder`). Detect diagnosis byte-verified: split-at-NUL yields `b""`, ASCII `2021.3.35f1` exactly at offset 48, v22 u32 @8, metadata_size 1668/data_offset 4096 present (two cosmetic imprecisions: 11 leading NULs not 12; string is NUL-terminated, not visibly length-prefixed). Numbers hand-checked: 140,329 dumps / 120.95 s ≈ 121 s / slowest level9 3.95 s / 51×exit-0 all sum from sweep-matrix.jsonl; S6↔R7 body-multiset identical (958 unique each); patched DataAchievements.txt = E1's verbatim 26-entry sample; X-1 cross-refs (790/1187, F3 at mono_typed_dump.py:250-257 raising unconditionally) verified. DEFECTS (none load-bearing): (1) doc says 47 containers carry "exactly 1" recursive warning — its own ledger gives level15=2, level17=2; (2) matrix r4 row records txt:0/xml:0 while its `-o out\r4-patched` holds 1542 txt + 1 xml (delta-count of pinned rerun; disk supports the doc); (3) the cyclic nested-tail caveat lives ONLY in the doc — no PROOF.md yet, nothing in EXTRACTION-LOG/census, and the proposed errata omits adding it to AC-12's enumerated residue list — must be landed by CodeWriter or it evaporates. AC-8 not threatened: truncation is bounded to dev-console cyclic fields (inspected instance has size-0 case lists); resources.assets ≥993 typed dumps + 26-entry array hold on evidence.
VERDICT: PASS
