# A-DS123-arbiter — build-readiness ruling on the three dataset specs

Chain held: reviews ds1/ds2/ds3 (NEEDS_REVISION 1/2/2) → fixes landed (vA diffed be04bf2..25d46a6, PASS) → I independently reproduced the load-bearing anchors: zero bare `<Name>MitaBlack</Name>` in any asset-list, `Location11_BlackMita`@level13.xml:3280 + `Location11_EyesMitaBlack`@:1552, `MenuPersonage.txt` sizes 10@48/14@301.
- **DS-1 characters: APPROVED** — every field sourced (§2 S1–S15), all six joins keyed with mechanism+status, ACs 1–10 executable repo-side incl. AC-7's transform-T negative grep; scope honest vs DummyDll bodies (R1 names IL-stubs, `preview_prefab_key` `[unverified]`, `gallery_icon` null-honest, portraits deferred to ART).
- **DS-2 achievements-endings: APPROVED** — the four-class taxonomy fences stripped-body reality up front; 15/26 `unverified-behavior` predicates are an explicit owner-deferred decision carried in-schema, not a silent gap; census 353-pure-all-wired + pinned exclusions and the `#line_index=` cite grammar both landed; ACs A1–A8/B1–B8 fail on wrong builds.
- **DS-3 dialogue: APPROVED** — §3.6 restated as union-of-carriers with the three failures + span levels 16/17/19/20, and the `line_index = indexString − 1` contract sits at §3.2/§3.6/D2 with the can't-catch-a-uniform-shift rationale; theme→Mita curation fenced as exactly 5 enums at D6 `null:"pending-curation"` (display names still ship); chapter join deferred to SPEC gap #1 while `level` emits now.
- Cross-spec consistency confirmed: chars stores speaker *names* only (J6/R1) while DS-3 attributes via the serialized `themeDialogue` enum + personage PPtrs — no stripped-body dependency, no contradiction; ending flags live in DS-2's logic tier in both texts; chapter handled by the same deferral; buildId 19029065/0.93L stamped uniformly.
- Non-blocking residue (resolve at emit-stage registration, same commit as the PIPE §3 tree update each spec already promises): (a) three sibling output conventions — `extracted/data/characters/` vs `extracted/datasets/` vs `extracted/dialogue/` — pick one family name; (b) `contracts/*.schema.json` drafts declared only by DS-2 — DS-1/DS-3 owe theirs (AGENTS rule 8 contract-pinning); (c) `relinks/locale_availability.jsonl` verified absent today — all three emitters must order after the P1 relink stage that owns it.

RULING: ALL_APPROVED — launch builders
