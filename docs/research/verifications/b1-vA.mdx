# B-1 verifier A — characters build, independent re-execution

Re-executed **7 of the 10 ACs** against the artifacts (AC-1/2/3/4/5/7/10) plus join-count reconciliation, not from the scoreboard: my own parser over `MenuPersonage.txt` reproduces sizes **10@L48 / 14@L301** and a **byte-equal tuple multiset** for all 24 rows; an independent full-field compare (**24 rows × 11 dump-backed columns**: paths, save keys, both loc indices, both RGBA quads, icon/case_back/button_menu PPtrs incl. file_id 2 shared player icon) shows **zero mismatches**; empty save_keys=2, empty resource_paths=9 as claimed.
**AC-2 locale parity:** 34 locales × 24 rows × 2 pointers = **1632 resolutions, all non-empty, zero U+FFFD**, per-locale parity 165 Menu / 26 Personages everywhere; `encoding-residue.jsonl` holds only LocationDialogue Location12 rows; `name_en` pivot == English resolution for all rows.
**AC-7:** transform T recomputes each claimed Name byte-for-byte (`MitaBlack`→T1 `Location11_BlackMita`, →T2 `Location11_EyesMitaBlack`), all 8 named-row locators grep clean **at their cited lines**, `<Name>MitaBlack</Name>` occurs in **zero** asset-list XMLs and is claimed verbatim nowhere; completeness probe finds verbatim equality **only** `MitaCore` ∈ {level15@13918, level17@20308} — no missed hard edges, so 2/14 named is exact; unnamed census triple-reconciles **emit == mb-dump filenames (incl. `_#NNN`) == asset-list `<Name>` counts = 93/45 · 31/31 · 17/17 · 3/3** (the loose-prefix 35 for Mob_ChibiMita are `Mob_ChibiMita_Animation*` clips, correctly outside the class boundary).
**Counts & joins:** personages 24 data rows ✓; candidates **26** (16 `evidence:[]`, 8 scene candidates whose cited files exist and quoted snippets grep clean, Flower/Ballerina text-mention-only) matching the build-log breakdown; outfit table 4 rows (stringNames 10–13); cartridge & achievement relinks each 22 fwd + 22 inv covering all 22 non-empty save keys with mechanism recorded; dialogue-speaker 3 fwd + 3 inverse, **zero attributions** (R1 honored).
**Row spot-check:** mita-dreamer (mtad, L88/L6, c1 0.2777/0.3552/0.7453, caseback 72, btn 2549), mita-black (mtaghost, L93/L12, caseback 112), player (plr1a, L119/L16, c1 blue, caseback 130), player-8 (empty path, plr8qeqe, L126/L23, black palette, shared icon 276) — every field matches the dump lines and resolves EN+RU correctly ("Sleepy Mita"/"Мита сонная" etc.).
**AC-6 recomputed independently:** n−1 yields Default/School/Vampire/Christmas; direct offset fails twice (13 out of range 0–12 ×34 locales; original→School) — pin + rejected_failure verbatim in outfit meta; **AC-8:** README carries the Version gap, both empty-icon facts, stub ladder; **AC-10:** Prankster/Long-legged/Mitaphone-class names appear in zero of the 24 rows.
Determinism: **no self-contained emitter exists repo-side** (pack-wide search outside `extracted/` found none), so per brief I verified statically instead — uniform key order across all 24 rows, the `_meta` ordering rule (mita 0–13 then players 0–9) holds, `name_is_shared` recomputes true for exactly the Menu-83 pair, preview_prefab_key rule holds; md5s pinned here for future reruns (personages `15365323b6c53a8f…`, candidates `932b6237ad58c72…`); the builder's own md5-identical rerun is therefore not independently re-executable by me — noted as a limitation, covered by the static properties above.
Deviations (relinks parked under `data/characters/relinks/` pending relocation, schema superset `case_back`/`button_menu`/`file_id`, `player-{n}` slugs, symmetric name_is_shared) are documented, spec-consistent (AC-3 explicitly demands S2-field survival), and none affects row truth.

VERDICT: PASS
