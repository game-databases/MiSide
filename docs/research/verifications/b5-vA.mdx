# B-5 verifier A — evidence re-execution (2026-08-25)

Re-executed AC-1/2/3/4/5: profile census == fresh `level2/MenuPersonage.txt` parse (multiset/order; row0 `mta` + row13 null; 11/2/1 at {6,9}/{13}); 11 placed pairs byte-equal vs own FlashTaker census AND B-4 `cartridges.jsonl`; 160 notes match filename census ×23 with dedupe arithmetic proven (258/98, md5 groups 25·25·25·23, 1 signature group); Personages 26×34 + Menu/Achievements 12/13 resolve ×34, zero U+FFFD; negative findings re-proven exact (ComicBook ×21, Translation `"-"`×1, 65 EN categories, 49 long m_Text / 18,117 dumps).
Spot-checks exact: row 0 true PathID **level17#13630** re-derived from raw level17 by my own UnityPy parse; paperpart #14090 field-for-field vs dump (GO 3470, indexPuzle 4, scrMain 19195); note-level5-11524 wiring 27/27 order-equal vs companion Events_Data/Time_Events dumps.
New claims confirmed: 73 components = 91 prefab-backed rows (23/23/23/22) + 69 singletons, chain proven via byte-identical resources.assets copy; zh-Hans/Hant lack all four Location19 book pages (32×8/8; 272/272 cells match D: tree); R2 sweep + falsifier in README; disk md5s == log pins; placement BY-REFERENCE (no duplicated payload, no coords, `_meta.placement_source` → DS-4).
Minors (none AC-bearing; fix at relocation commit): log's "note wiring = 185" mislabels — notes carry 183, 185 is set-wide incl. novella's 2 pause calls; `document--minigame.jsonl` writes mechanism text into `status` on all 12 rows instead of modeled/partial/missing. AC-9 accepted on pinned hashes + checker presence, not re-run (read-only scope).
VERDICT: PASS
