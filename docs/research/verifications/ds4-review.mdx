# DS-4 review — dataset-cartridges.mdx (2026-08-25)

Verified on corpus: C1 @`0x13AD860` splits to exactly **23 keys**, claimed order +
13/10 family split byte-exact (entry stride 4 lines ⇒ array index 4223 confirmed);
fresh FlashTaker census = **21** pickups, levels 8–21, matching §3.1 pair-for-pair
with mtad2/mtacore absent (tier-2 honesty holds); TV −1 pin coherent (`Fight`=1/
`Pinguin`=2 vs TelevisionGames lines 0–1); automate field values, asset-list
exclusivity (incl. Tetris breadth 3–22, GamesCore 15+17), Arabic MakeManeken 34/35,
DEC stubs body-less (⇒ J6/R1 scoring downgrade justified + ledgered), C13 = 22
anchors ×2 dirs all `logic` (no `flashes:mta`, consistent w/ empty nameSave),
greatdance award site, Menu `#line_index=61–62`, "Hetoor" grep **zero hits** — all
reproduce. Cross-spec: no row double-ownership vs DS-5; boundary agrees, 21
placement facts agree byte-for-byte (see F3).
- F1 MAJOR — §3.5/R4/AC-7 claim "most MinigamesController.txt dumps byte-identical;
  level2's differs" is false: 48 dumps hash into **4 groups (19/16/12/1)**; level2's
  group has 12 members. Restate the measured clustering; AC-7 stays mechanical, drop
  the "level2's distinct copy survives" expectation.
- F2 MINOR — §4 tree says minigames.jsonl "~16 owned rows"; §1/§3.2 measure **17**.
- F3 MINOR — DS-5 §1.2 routes pickup identity to DS-1 §5-J5 and never cites DS-4;
  the placement fact is emitted by both specs without a shared-source ruling — add
  bidirectional cross-references before either emit stage lands.
- INFO — cite gallery labels in `#line_index=` grammar; pre-ledger that the `mta`
  row's `depicts_character_id` is null-by-C13-absence.
ACs sampled (1, 2, 4, 7, 8): repo-only, runnable, discriminating; AC-7 blocked on F1.
VERDICT: NEEDS_REVISION — 3
