# v-ds4 — F-DS4 fixes verified on commit 3cfdd1c

F1 ✓ AC-7/R4/§3.5 assert the measured 4-group hash partition 19/16/12/1 (=48) mechanically (re-hash → reproduce recorded partition); old byte-identical-template + level2-outlier claims fully deleted — only remaining "privilege" text is AC-7's negation. F2 ✓ 17 everywhere (§1; §3.2 3+2+8+4=17; §4 tree); no stray "~16" (remaining ~17s are the SPEC/D1 estimates being superseded).
F3 ✓ §1 names DS-4 the **single placement authority**, DS-5 consuming by reference with pre-emit citation commitment; §6 carries emit-ordering + id-join counterpart (`depicts_character_id` ↔ DS-5 `subject_character_id`); consistent with DS-5 §1.2 (pickup identity→DS-1-J5 and P5-gated transforms untouched — different facts, silence filled not contradicted).
Scope ✓ diff touches `dataset-cartridges.mdx` only; hunks map 1:1 onto F1/F2/F3 (+ its own brief file).
Caveat (non-blocking): §1's "the 21 pairs appear … in DS-5's `placement` column" overstates — that column covers only the 11 Mita-side placed rows (10 `plr*` placements sit in DS-5 §3 prose, DS-1/DS-2-owned); scope the byte-for-byte reconcile to the overlap or a future verifier fails DS-5 spuriously.
VERDICT: PASS
