# R-C-W1 — Code Review of C-W1's `run_all` implementation

1. **BLOCKER** `pipeline/stages/detect.py:107-118` — census-drift gate raises unconditionally; `ctx.expect_drift` (set at `run_all.py:146`, `common.py:89`) is read by **no** stage. `--expect-drift` is advertised in `--help` yet cannot get past the very gate it exists for; patch-day rerun (S2's stated purpose) hard-fails until someone hand-edits `detect.json`.
2. **MAJOR** `pipeline/stages/mono_typed_dump.py:177-178,271-278` + `decompile.py:175-176,98-101` — on FAIL-FAST mid-run the ledger/stage-report is never written (both emit only after the loop), while output trees were already wiped at stage start: the **previous** run's complete `sweep-attempts.jsonl` / `decompile.json` survives beside a half-wiped corpus, and S8 reconciles clean against missing dumps — precisely the "half-swept corpus never looks complete" hazard S4's write mode exists to prevent. Rewrite (or retract) the ledger on the abort path too.
3. **MAJOR** `pipeline/stages/detect.py:160` — `free_space_bytes` lands in `census/stage-reports/detect.json` but is absent from `VOLATILE_FIELDS` (`common.py:37-42`); free space nearly always differs between consecutive runs ⇒ AC-5's double-run byte-diff fails on any real rerun.
4. **MAJOR** `pipeline/stages/art_export.py:69-70` vs `357-363` — `.psd` classifies as `psd-other` before the `Data/Languages` branch can ever claim it, so the `psd-source` subset scan over `languages-art` is dead code and AC-10's "per-locale art 777 png **+ 3 psd**" is not expressible in the catalogue (totals still reconcile via the top-level row).
5. **MINOR** `mono_typed_dump.py:263-278` — probe attempt row is written first, but levels sit last in sweep order; spec S4 pins "one row per container **in sweep order**" (deterministic, off-spec ordering).
6. **MINOR** `mono_typed_dump.py:105-108` — `_loaded_containers` substring-matches names, so `level1` "loads" whenever `level13` appears; deviation-6 attribution ledger is wrong for nested-number containers.
7. **MINOR** `art_export.py:39,157-160` — staged texture scope derives from an invented 3 GiB *stream-bytes* bar, not the ruled first-pass **row counts** per family (spec S6 / questions §6); disclosed in EXTRACTION-LOG and lands on "export" for the real install, so contained.

Command fidelity otherwise verified: Il2CppDumper argv+cwd-pinned scratch config delta per run (`il2cpp_dump.py:82-84`); AssetStudioModCLI input strictly argv[1], E1 flags verbatim (`_dump_argv`); venv/pip shape and 13-pin freeze byte-match E1; `ilspycmd -p -o <outdir> <dll>` pinned at build time per §8. No write touches `<game-root>`; ledgers use deterministic full rewrite; `.gitignore` gains `extracted/il2cpp/`; exit codes 0/2/3/4 behave (smoked: `--list`, `--help`, three usage errors → 2; deps→4 and stale-pin→3 paths read correct in code, TestWriter's tier). `C-W1.log` claims check out against the code.

VERDICT: NEEDS_REVISION — 7 findings
