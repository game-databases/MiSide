# VC-4 — Adjudication of F-VC3 (fresh critic, production build `lSGP9RwBly4qk0TO09HesAug`, own port 4942)

Method: build freshness proven (zero `src/` files newer than BUILD_ID; only regenerated `public/api|search|map` data planes are newer, served live from disk). All 5 fixer claims re-proven against rendered DOM: full-id-vocabulary text walkers (23 save_keys + 17 minigame_ids + snake_case sweep) over 4 Mita routes, CDP real-pointer hovers, typed header search + Escape, `getAnimations()`, curl status/title sweeps; competitor chrome re-shot today (enka 21 imgs, entity-titled · wikily home 49 · NWG item 56 imgs, heads Crafting/Calculator/3D/Images/Comments).

1. **Raw ids — CLEAN.** Zero vocabulary hits, zero snake_case tokens in visible copy on `/mita/mita-kind`, `mita-cap`, `mita-chibi`, `mita-black`; chips render pickup-scene/collectible-set under an explicit never-falls-back-to-save_key rule ([EntityDetailRoute.tsx](../..//site/src/components/routes/EntityDetailRoute.tsx)); sole corpus trace remains the RSC flight payload.
2. **Cartridge density — YES.** `/cartridges/mtakd` prints module counts Mitas 1 · Locations 1 · Minigames 2 and renders pinned joins as links — Kind Mita, CappieLevel 17, Games Core MainCore Terminal, Tetris GameScene Prop — all HTTP 200.
3. **Grid art census — DESIGN RULING UPHELD AND IMPLEMENTED.** Cards arted: minigames 15/17, cartridges 10/23, endings 3/4 (fixer numbers exact); players 0/10, locations 0/24 honest-empty. Ruling: honest-empty beats a tasteful placeholder tile — placeholder art is invented content (pack non-goal) and a stock-art substitute (network icon rule); the binding bar is design-standard §5.1/§5.2 "empty wells … designed compositions", and it is met: artless cards paint the checkerboard conic-void over #4a1d5a (VoidWell recipe) and every card on all five indexes measures exactly 268px, so rhythm never breaks.
4. **Dialogue `<title>` — YES** on both trees: "Dialogue — MiSide Database" / «Диалоги —» / "Dialoge —".
5. **Card lift — YES**: computed transform `none` → `matrix(1, 0, 0, 1, 0, -2)` under a real CDP pointer.
6. **No round-3 regressions:** typed "mita" returns 54 rows across 8 kinds (histogram identical to VC-3), main hidden-not-unmounted, Escape restores main text exactly; corruption hover fires the CRT split `rgb(8,203,5)/rgb(255,0,156)` on 4 corrupted minigame cards; VHS 404 (status 404) runs `ms-vhs-drift`; SPACE keycap live in the opened DialogueBand footer; home transcript rail prints node counts (nodes:63 Chibi Mita … nodes:567 Beyond the World); `/ar/mita/mita-kind` serves `lang="ar" dir="rtl"` at 200; RU entity titles render («Мита 2D»).

Grades (Δ vs VC-3): Home **8**(+0) · Entity detail **8**(+2) · Search-in-place **9**(+0) · RU **9**(+0) · AR **8**(+0) · Chrome/motion **9**(+1).

Still leading: header search-in-place vs NWG's navigate-away results and enka's none; corruption-as-state + VHS drift remain interaction no competitor shows; locale URLs incl. RTL ar at 200 beat enka's client-side switch. Still losing: NWG's item page stacks Calculator/3D/Images/Comments modules over our relink-only cartridge set; wikily/NWG image density (49/56) vs our cartridge grid 13/23 artless.

Backlog, not gates: extend the cartridge portrait join toward the 13 artless rows; NWG-class interactive modules arrive with tools-plan/UGC phases, not this surface.

Evidence probes: throwaway step files deleted; ports 4942/9362 released port-scoped after capture.

CRITIC: CHROME_PASSES
